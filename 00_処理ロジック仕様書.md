# 第二の脳 — 処理ロジック仕様書 v1.0

作成日：2026-07-04
最終更新：2026-08-05（タスク生成時に branch / tags を付与可能に：Knowledge と同じ検索体験を目指す）
対象：Claude Chat / Claude API / Claude Code など、どの実行環境でも共通して使う処理ルール

---

## 0. 設計思想

目的は情報を保存することではなく、**日々の経験・学習・思考を構造化し、将来の意思決定と事業（コンサルティング）に再利用できる知識資産へ育てること**。

インプット窓口は1つ。すべての音声・URL・テキスト入力を、性質の異なる2つの出力構造に分解する。

- **マインド（Mind Graph）**：思想・価値観・性格特性・仮説など、多軸で自由に関連し合う知識。テーマ横断こそが価値。
- **スキル・アセット（Skill/Asset Tree）**：ツール活用法・事業運営スキル・専門知識など、体系的に積み上がる資産。将来のブログ・コンサル資産の種。

この2つは同じ入力から同時に抽出されることもある（例：ジャーナル1件からマインドの気づきと、ツールの学びの両方が出る）。

---

## 0.5 保存構造の設計原則（2026-07-19 確定）

このツールの大きな利点は、**リレーショナルDBを持たず、ファイル置き場（Google Drive等）さえあれば動く**ことにある。この利点を活かすため、以下を設計原則として確定する。

**原則：Note・Knowledge・Task・Great Mind・Habitは、すべて1オブジェクト＝1ファイルの独立ファイルとして保存する。**

- Note（Journal/Source/Insight/Inbox）は、その日その場でインプットされた情報の「入れ物」であり、それ自体も1ファイル
- そこから抽出されたKnowledge・Task・Great Mindは、Noteのfrontmatter内に配列として埋め込むのではなく、**専用フォルダの中に、それぞれ独立したファイルとして保存する**
- Habitも同様に独立ファイル。ただしTask同様、ジャーナルや記事から良い習慣を発見して登録することがあるため、`related_notes`で発見元Noteを辿れるようにする

**なぜこの方式にしたか（一度、逆方向で失敗した経緯）**：
2026-07-19、一時的に「Great MindはUI上の専用タブが不要だから、ファイルとしても軽量化してNoteに埋め込んでよい」と判断したが、これは誤りだった。**「UI上どれくらい頻繁に見るか」と「ファイルとしてどう保存するか」は無関係の話**であり、埋め込み方式は以下の代償を伴っていた。
- 検索性の低下：ファイル一覧だけでは中身が分からず、全Noteを開いてYAMLを解析しないと集計できない
- 参照の脆さ：埋め込み方式ではIDを持たせにくく、タイトル文字列で参照すると、タイトルを1文字直すだけでリンクが切れる

独立ファイル化により、この2つの代償を解消した。UI上の見せ方（専用タブを持つか、他画面から時々参照する程度か）は、保存方式とは別レイヤーの話として、都度決める。

**各オブジェクトの「出典・親子関係」の持たせ方**：
- 子オブジェクト（Knowledge/Task/Great Mind/Habit）は`origin_note`フィールドで、生まれたNoteのidを持つ
- 親であるNote側にも、`knowledge_refs` / `task_refs` / `great_mind_refs`という**IDだけの軽量な配列**を持たせ、Noteを開けば何が生まれたか一覧できるようにする（内容の二重管理はしない。実体は子オブジェクト側のファイルのみ）

**フォルダ構成（更新後）**
```
00_Inbox/        未整理・クイックメモ
01_Journal/      日々のジャーナル
02_Sources/      外部情報（Video/Article/Book/Work）
03_Insights/     パターン・実験ログ
04_MOC/          テーマ別ハブ（4テーマ）
05_Reference/    スキル・アセットツリー
06_GreatMind/    賢者の教え（独立ファイル、1ルール1ファイル）
07_Task/         タスク（独立ファイル、1タスク1ファイル）
08_Knowledge/    ナレッジ（独立ファイル、1件1ファイル）
09_Habit/        習慣（独立ファイル、1件1ファイル）
Templates/
```

---

## 1. インプット処理の優先順位

Knowledge生成時、以下の優先順位で扱う。

1. **Reflection**（ユーザー自身の感想・解釈・経験）— 最重要
2. **Source Summary**（記事・動画の要約）
3. **元ソース**（URL本文・動画内容そのもの）

Reflectionに根拠が薄い場合はconfidenceを下げ、必要に応じてResearch Taskを生成する。

### URL取得ルール
- 記事：本文取得を試みる。成功時はTitle/URL/詳細要約を生成。失敗時は「本文を取得できませんでした。Source Summaryを入力してください」と返す
- YouTube：動画内容の取得は保証されない。取得できない場合、要約記事等の代替ソースを探す。それでも得られない場合はSource Summaryの入力を求める

---

## 2. マインドグラフ（Mind Graph）仕様

### 2.1 テーマ（Territory）— 4分類、固定
- Healthcare（健康）
- Business（事業）
- Mind（マインド：考え方・価値観・性格特性）
- Relationships（人間関係）

1つのアトムが複数テーマに属してよい（例：睡眠は healthcare × business）。

### 2.2 カテゴリの枝（Branch）
テーマの下に、内容に応じたカテゴリを持つ。例：
- Healthcare：睡眠／食事／運動／日光浴・スキンケア
- Business：戦略
- Mind：マインドセット／性格特性／学習法

**マインドセットと性格特性は固定区分ではなくスペクトラム**。状況依存の考え方（マインドセット）が繰り返されて性格特性に「昇格」したり、逆に性格特性だと思っていたものが状況次第の柔らかいマインドセットへ「降格」したりする。今後、この移行を表現できるようにする。

新しいカテゴリが必要そうだと感じたら、Claudeから「これは新しい枝になりそうです」と提案する。ユーザーの承認を得てから追加する。

### 2.3 知識アトム（Knowledge Atom）
**ルール**
- 1ノード1主張
- タイトルは結論形式（例：「小規模企業共済は高所得者ほど節税効果が大きい」のような言い切り文）
- 定義だけのノードは作らない。再利用可能な内容のみ抽出
- 事実だけでなく、経験から得た原則・仮説・将来検証したい考えもアトムとして保存する
- **Knowledgeは一度書いたら完成ではない。Noteと違い、複数のNoteから知見が積み重なるたびに育っていく存在**（詳細は2.3.1）

**スキーマ（独立ファイル：`08_Knowledge/{id}.md`。2026-07-19より、Noteへの埋め込みではなく独立ファイルに変更）**

| フィールド | 内容 |
|---|---|
| title | 結論形式の短いタイトル（グラフ表示用） |
| summary | 現時点での最良の理解（詳細表示用）。1つのNoteだけの解釈ではなく、`evidence`に積み上がった根拠を踏まえて書き直されていく本体 |
| evidence | 背景・根拠を積み上げていくログ（削除せず追記。詳細は2.3.1） |
| themes | 所属テーマ（複数可） |
| branch | 所属カテゴリ |
| confidence | 信頼度 0〜100 |
| source_quality | High / Medium / Low（複数evidenceがある場合は最も質の高いものを採用） |
| created | 最初にこのKnowledgeが生まれた日 |
| updated | 直近でevidenceが追加された日 |
| origin_note | 最初にこのKnowledgeを生んだNoteのid（0.5節） |
| related_notes | それ以降、evidenceとして関わった関連ノート |
| revises | （該当する場合）どのKnowledgeの考えを更新したか |
| merged_into | （該当する場合）このKnowledgeが統合された先の正規Knowledge id。統合後は新規evidenceを持たず、参照専用になる |

**注記**：「Knowledge」と「アトム」は同じ概念を指す（設計初期はアトムと呼んでいたが、Knowledge Builder参考時にKnowledgeという呼称も混在した。以後はKnowledgeに統一する）。titleはグラフ上で表示する短い結論文、summaryはタップして開いた時に読む「なぜ・どうなった」を含む本体。親Note側には`knowledge_refs: [id, ...]`という軽量な参照リストのみを持たせる。

**confidenceの目安**
- 90〜100：高品質ソースで裏付け済み
- 70〜89：比較的信頼できる
- 40〜69：一部推測を含む
- 0〜39：主観・仮説中心

**source_qualityの目安**
- High：学術論文／公的機関／専門書
- Medium：企業ブログ／一般書籍／YouTube
- Low：個人的体験／感想／個人ブログ

### 2.3.1 Knowledgeは育つ ― summary と evidence の役割分担

**背景にある考え方**：URLの解釈や1回のジャーナルだけから生まれたKnowledgeは、まだ「質の高いナレッジ」とは言えない。いろいろな知見が複数のNoteをまたいで折り重なることで、初めて見返す価値のあるナレッジに育っていく。NoteはURL・動画の要約など、その時点の入力を丸ごと保存する「一次資料のアーカイブ」であり、リッチだが原則書き換えない。一方Knowledgeは、Noteほどのボリュームは要らないが、**時間をかけて深掘りされ、太っていく**べき存在。

**`evidence`のフォーマット**（追記型ログ）
```yaml
evidence:
  - date: "2026-07-02"
    source: "20260702-source-01"
    note: "動画で推奨されていた23時就寝と一致することに気づいた"
  - date: "2026-07-02"
    source: "20260702-journal-01"
    note: "実際に60日間継続できており、以前より睡眠の質・日中パフォーマンスが向上したと感じている"
```

**運用ルール**
1. 似たKnowledgeが新しいNoteから見つかったら、新規Knowledgeを作らず、既存Knowledgeの`evidence`に1件追記する（2.6の自動強化と連動）
2. `evidence`が増えるたびに、Claudeは`summary`を**書き直す**（積み上げるのはevidenceだけ。summaryは常に「今のところの最良の要約」を保つ）
3. `evidence`が1件しかない（＝1つのNoteの解釈だけに基づく）Knowledgeは、まだ育っていない状態。confidenceが低い場合は特に、Research Task「この知見をさらに深掘りする」を自動生成し、深掘りを促す
4. Note詳細画面・Knowledge詳細画面のどちらでも、`evidence`は時系列のタイムラインとして表示する

### 2.4 Summary Knowledge
1つの入力（ソース）ごとに、最も重要な学び・原則・価値観を1つ抽出し、その入力から生まれた複数のKnowledgeを束ねる上位ノードとして作成する。

### 2.5 リンクの種類
| 種類 | 意味 | 表現 |
|---|---|---|
| hierarchy | テーマ→枝→Knowledgeの階層 | 実線・グレー |
| cross | テーマを横断する関連性 | 破線・紫 |
| revises | 過去のKnowledgeの考えを更新した | 矢印付き実線・オレンジ |
| source | Knowledgeの抽出元 | 点線・グレー |
| experiment | 同じ行動変化に紐づく複数の観測結果 | 破線・紫（太め） |

### 2.6 出現頻度による自動強化
似たような（完全に同一でなくてよい）知識が複数ソースから得られた場合、新規Knowledgeを作らず既存Knowledgeへ統合・強化する。その際`evidence`に1件追記し、confidence・関連ソース数を更新し、`summary`を書き直し、リンクを太く／ノードを大きく表示する。

**この自動強化とは別に**、複数ノートで構造が同型の葛藤・パターンが独立に出現した場合の「Insightノートへの昇格」は、回数の閾値ではなく、Claudeが「明らかに強い関心・繰り返しの重み」を感じたときに手動で提案する。提案時は判断理由を必ず添える。

### 2.7 実験ログ（Experiment Log）
同じ生活習慣・行動の変化について、複数の異なる症状／観測結果が時間をかけて蓄積される場合、単発のKnowledgeではなく「実験ログ」として追記型のノードに育てる（例：夕食抜き実験ログ）。日付ごとに観測を積み上げる。仕組みとしては2.3.1のevidenceログと同型（実験ログは複数の異なる症状を横断的に束ねる点が異なる）。

### 2.8 Great Mind（賢者の教え・背景情報）

**位置づけの経緯（2026-07-19、2回の見直しを経て確定）**：
1. 2026-07-13時点：専用タブは不要だが、独立ファイル（`06_GreatMind/`）としては残す
2. 同日中に一度：「専用タブが不要＝ファイルも軽量化すべき」と誤って判断し、Note内埋め込み方式に変更
3. **最終的に②を撤回し、①に戻した**：保存構造（独立ファイルにするか）とUI上の見せ方（専用タブを持つか）は無関係の話だと整理し直したため（詳細は0.5節）

- **`06_GreatMind/`フォルダに、1ルール＝1ファイルとして保存する**（独立ファイル・独自の`id`を持つ）
- `origin_note`フィールドで、その教えが最初に登場したNote（記事・動画のSource Note、または他に出典がなければJournal）のidを持つ。従属する側のNoteには、`great_mind_refs`という軽量な参照リストを持たせる
- ユーザー自身の解釈・当てはまり度合いは、これまで通りGreat Mind側には持たせず、**それを引用するKnowledge側の`alignment`フィールド**にのみ記録する（この方針は一貫して変更なし）
- Great Mindを一覧として見返す専用画面は、UI上には（あっても）ほぼ使わない想定。「たまに参照する背景情報」として、メンターコメント（2.9）が主な露出経路になる

**スキーマ**
```yaml
id: "（例：20260706-source-02-gm-01）"
schema_version: 1
type: great_mind
title: "（1つの行動指針・ルール。「〇〇する」という習慣文）"
sage: "（賢者名）"
sources:
  - title: "（動画・記事タイトル）"
    url: "（URL）"
rationale: "（なぜ重要とされているか）"
precondition: "（有効になる前提条件）"
pinned: true | false
origin_note: "（生まれたNoteのid）"
visibility: private
```
出典が明確でない場合（伝聞・要約からの抽出等）は、`source_note:`という一言フィールドで由来を書き添えてよい（例：「〇〇さんが△△と語った内容から」）。

### 2.9 鏡・メンターとしてのフィードバック

**コンセプト**：このツールは「自分を映し出す鏡であり、メンターである」という位置づけ。alignmentの生データをテーブルで見せるのではなく、**温かい一言コメント**として合成し、アプリを開いた時に自然に目に入るようにする。

例：「昨日のジャーナルは『最重要な1つに時間と労力を集中させる』というマインドセットを体現していていいですね！」

**仕組み（案）**：
1. 直近のJournal/KnowledgeのうちalignmentがついているものからPickする
2. stanceが`agree`のものを優先的に肯定的なコメントとして表示する（`disagree`/`tension`の場合は、否定するのではなく「〇〇については迷いがあるようですね」のような、思考を後押しする言い方にする）
3. 表示場所は①のCapture画面（アプリを開いて最初に見る場所）を想定
4. コメントは1日1〜2件程度に絞り、通知過多にならないようにする

### 2.10 将来構想：Great Mindの共有ライブラリ化
現在はユーザー本人が気に入った賢者のGreat Mindを個人のVaultに登録していく形だが、将来的には**参考文献がしっかりしているGreat Mindに限り、他のアプリ利用者からの登録も受け付け、共有ライブラリとして誰でも選んで使えるようにしたい**という構想がある。

これは技術的には、個人のGoogle Drive/GitHubに閉じたデータではなく、**全ユーザー共有のデータベース（例：Supabase等）が必要**になる、フェーズ2以降の話（前回のインフラロードマップの「20人〜」フェーズに相当）。今は個人のVaultにNote単位で埋め込む形を徹底し、共有化は将来の課題として持ち越す。

### 2.10.5 習慣記録フロー設計ビジョン（2026-07-28 ユーザーFB、07-28 詳細化）

2.11〜2.12 で Habit のスキーマと基本仕様を定義済みだが、**実際の朝・夜のユーザー動線から見た Habit 記録フローのビジョン**が 2026-07-28 のユーザーFBで示された。本セクションは「原則として」レベルであり、実装詳細は今後のセッションで詰める。実装が完了した暁には 2.11〜2.12 と統合するか、本セクションを正式仕様として昇格させる。

#### 朝のフロー

1. アプリ起動 → **Capture タブの morning gate**（既に実装済み）
2. **「おはよう」ボタン**をタップ → 起床時間を記録
3. その後の画面（**ホームタブ**）で**起床時間を変更可能**にする（「おはよう」を押した直後の判定時刻を編集できる、遅刻して押したケースへの対応）

朝のうちにやること（可能性）：
- ジャーナル記録（Capture タブ）
- グラフ表示（探索タブ）で自分のナレッジ確認

#### 夜のフロー

1. その日1日の振り返りジャーナル記録（可能性、強制ではない）
2. **ホームタブの右上に「おやすみ」ボタンを常設**
   - 19 時前は無効化（押せない、薄い色）
   - 19 時以降になると活性化する
3. 「おやすみ」ボタン押下 → **夜の門番画面**（morning gate と同じ構造で、内容を「おやすみ」に差し替えたもの）に遷移
4. **登録されている習慣を、今日は実行できたか回答**（フリック UI／仕様書 2.11）
5. **就寝予定時刻を 5 分刻みで入力**
   - デフォルト：`appSettings.targetSleepTime`（設定済みの就寝時刻）
   - それより現在時刻が過ぎていた場合：5分区切りで**今より一番近い未来の時間**（例：23:47 → 23:50）
6. 「おやすみ」ボタン押下 → **ホームタブに戻る**（記録完了）

#### 19 時以降の morning gate の挙動

- 朝の門番画面（Capture タブ）自体は常時アクセス可能だが、**19 時以降になると「設置項目が切り替わる」**
  - 「おはよう」ボタンが「おやすみ」ボタンに切り替わる
  - 朝：起床時刻入力 → ホーム遷移
  - 夜：就寝予定時刻入力（5分刻み）→ ホーム遷移
  - 朝と夜で**同じ画面・同じ配置**を使い回し、内容だけ差し替える（実装効率と一貫性のため）
- ホームタブの「おやすみ」ボタン（右上）は独立して常設。19 時前から薄く表示して存在を示してもよい

#### 日中の記録（HOME タブに集約、2026-08-04 確定）

- 2026-08-04 セッションで **Tasks タブ Daily サブタブを廃止**。Tasks タブは Ad Hoc のみ
- 日中記録は **HOME.todaybrief のウェイト実績セクション**で完結する
- 起床時刻・就寝予定時刻は HOME.todaybrief 内で編集可能（変更ボタン押下で Drive 保存 + チェックマーク表示）
- ウェイト実績は簡略版（今日の完了チェック + 14日カレンダー + 連続日数）のみ
- ホームタブ / おやすみ時の門番画面 の **2 つの経路から同じ Habit データに記録可能**な設計とする（Tasks タブ Daily は廃止）
- 旧 `renderDailyHabitList` は関数自体残置（呼び出し箇所なし）、HOME 用は `renderTodayBrief` 内のウェイト実績描画ロジックで代替

#### 起床・就寝時刻のスキーマ（時刻を可視化するため）

起床・就寝の判定時刻を `log[today] = true` だけで持つと、いつ起きたか／いつ寝る予定かが分からない。**リズムを後から可視化するため**、時刻そのものを明示的に記録する：

```yaml
type: habit
id: "h-wake"
title: "起床"
special: "wake"
themes: ["healthcare"]
log:
  "2026-07-28": true
wake_time:                              # 新しいフィールド：毎日の起床時刻
  "2026-07-28": "05:23"                 # ISO 8601 時刻文字列（"HH:MM"）
  "2026-07-27": "05:30"
created: 2026-07-04
status: "active"
```

```yaml
type: habit
id: "h-sleep"
title: "就寝"
special: "sleep"
themes: ["healthcare"]
log:
  "2026-07-28": true
sleep_planned_time:                     # 新しいフィールド：毎日の就寝予定時刻
  "2026-07-28": "22:30"
created: 2026-07-04
status: "active"
```

- `wake_time` / `sleep_planned_time` は **`{ "YYYY-MM-DD": "HH:MM" }` 形式の dict**
- 既存の `log` とは独立に保持（`log` は「実行できたか」の Yes/No、`wake_time` は「いつ起きたか」の時刻）
- パーサー側で `fm.wake_time || {}` / `fm.sleep_planned_time || {}` として読み込み
- 後で見返した時、「7月の起床時刻は平均 5:28 → 7月下旬は 5:45 に遅くなっている」のような**リズム分析**に使える

#### スキーマの拡張：既存パーサーの修正

- `loadHabitFromDrive`（line 3083）で `wakeTime: fm.wake_time || {}`, `sleepPlannedTime: fm.sleep_planned_time || {}` を追加
- `persistHabitChange` で `{ wake_time: {...}, sleep_planned_time: {...} }` を渡せる（既存パターン通り）
- `camelizeKeys` ヘルパー（4.4.5 節）でメモリ上は `wakeTime` / `sleepPlannedTime`、YAML 上は `wake_time` / `sleep_planned_time`

#### 設計ビジョンで残った未確定 / 今後詰める項目

2026-07-28 ユーザーFB で以下 5 件は確定済み（実装着手フェーズに移行）：

| 項目 | 確定内容 |
|---|---|
| Daily サブタブの Habit UI | **リスト形式シンプル、チェックボックスで「完了」ボタン**を押す方式（カードスタック式は不採用） |
| 19 時切替ロジック | **ハードコード 19:00 で OK**（設定項目化は将来課題） |
| 5 分刻み UI | **時刻ピッカーで 5 分単位**。ユーザー設定・入力のすべてで同じ入力方式に統一（target_wake_time / target_sleep_time / wake_time 編集 / sleep_planned_time 編集 すべて同一 UI） |
| 「おはよう」「おやすみ」UI 切替アニメーション | **既存のおはようアニメーション（鳥・風船・シャボン玉の burst）を踏襲**。`spawnBurstElements`（line 4533 付近）のパターンを使い回す |
| リズム可視化 | **HOME タブに実装済み（2026-08-04）**。`renderRhythmChart` で Canvas 描画。生活リズム（過去 30 日）セクションに平均起床・平均就寝 + 折れ線グラフを表示 |

#### 実装済み：リズム可視化（HOME.todaybrief、2026-08-04 セッション）

`wake_time` / `sleep_planned_time` の可視化は HOME タブの生活リズムセクションに実装済み。`renderRhythmChart`（index.html line 4327-）が Canvas で折れ線グラフを描画。

**設計判断（確定）**：

| 項目 | 確定内容 |
|---|---|
| 表示場所 | HOME.todaybrief（起床・就寝入力欄の直下） |
| 集計範囲 | APP_TODAY から過去 30 日 |
| 起床軸（左） | **動的計算**：データ min - 1h 〜 max + 1h（最低 3 時間幅を確保）。データ点 1 つの場合は center ± 1.5h で 3 時間幅を確保。データなし時は固定値 4:00-9:00 |
| 就寝軸（右） | **動的計算**：起床と同じロジック。24 時超え表記（24:00, 27:00 等）を維持。データなし時は固定値 21:00-27:00 |
| 軸ラベル | **動的計算**：3〜5 個になるよう 30/60/120/180/240 分刻みで範囲に合うものを採用。`ceil(min/step)*step` から開始 |
| 配色 | 起床=オレンジ `#f59e0b`、就寝=紫 `#a855f7`。平均起床・平均就寝の背景色・数値文字色も同系色（rgba(245,158,11,0.15) / rgba(168,85,247,0.15)）で統一 |
| 横軸 | 5 日刻み（25, 20, 15, 10, 5, 0日前）。「0日前 = 今日」表記 |

**APP_TODAY の動的化（2026-08-04 セッション）**：

リズム可視化を実装したところ、ユーザー入力データ（h-sleep.md 等）がグラフに反映されない問題が発覚。原因は `APP_TODAY = new Date("2026-07-08")` という固定値で、過去 30 日分のキーを `2026-06-09〜2026-07-08` で参照していたため。ユーザーの実データは `2026-07-09〜2026-08-04`（現在日付周辺）で参照範囲外だった。

**対応**：`APP_TODAY` を日本時間 (Asia/Tokyo) での現在日付に動的化：

```javascript
const _appNow = new Date();
const _appJst = new Date(_appNow.getTime() + 9 * 60 * 60 * 1000);
const APP_TODAY = new Date(Date.UTC(_appJst.getUTCFullYear(), _appJst.getUTCMonth(), _appJst.getUTCDate()));
```

- ブラウザのタイムゾーンに依存しないよう UTC+9 オフセット加算 → UTC メソッドで年月日取得
- `APP_TODAY.toISOString().slice(0,10)` で従来通り `"YYYY-MM-DD"` を返す
- テスト再現性が必要な時は一時的に固定値に戻せばよい（コメントで手順明示）

**影響範囲**：`APP_TODAY` は Habit の日付キー生成、`daysSince`/`daysUntil` の基準日、Task `created` のデフォルト値などで使われているため、すべて「実今日」基準に切り替わる（意図通り）。

#### 2026-08-02 実装：HOME.todaybrief への就寝予定時刻欄追加

ユーザー手動テストで「起床時刻欄は表示されるが、就寝予定時刻欄が表示されない」バグが発覚。
原因は `renderTodayBrief` の `brief-wake-sleep` に就寝予定時刻欄が描画されていないことだった
（旧コメントに「就寝予定時刻はおやすみ画面で入力」とあり、実装意図として朝 HOME.todaybrief
には載せない方針だったが、ユーザー観点では朝から就寝予定を確認・編集したい場面が多い）。

**設計判断（確定）**：

| 項目 | 確定内容 |
|---|---|
| 表示場所 | HOME.todaybrief の起床時刻欄の直下（縦並び） |
| UI 構造 | 起床時刻と完全に対称（`disabled` + 「変更」ボタン + 5 分刻みプリセット） |
| プリセット値 | おやすみ画面と同じ `["22:00", "22:30", "23:00", "23:30", "00:00"]` の固定値（`computeSleepPresetTimes` 関数） |
| 編集フロー | `enableSleepPlannedTimeEditOnHome` で input を有効化 → onchange で既存 `updateSleepPlannedTimeNow` 呼び出し → `persistHabitChange` で Drive 同期 |
| Drive 復元 | 既存パス `refreshHabitFromDrive` が `sleepItem.sleepPlannedTime` を `sleepHabit.sleepPlannedTime` に Object.assign でマージ（Task 76 で実装済み） |
| おやすみ画面の UX | 変更しない（おやすみ画面は「これから寝る」フローで時刻確定済みのため `disabled` + 「変更」ボタンは不要） |

**実装内容**：index.html 26 行追加（コミット `4348fc2`、push 済み）

- `renderTodayBrief` 内に就寝予定時刻欄の HTML を追加
- `enableSleepPlannedTimeEditOnHome` 関数を新設（`enableWakeTimeEditOnHome` を踏襲）
- `computeSleepPresetTimes` 関数を新設（5 件の固定プリセットを返す）

**動作確認手順**：

1. Google Drive に接続
2. HOME.todaybrief 画面に「🛏️ 就寝予定」欄が表示されることを確認
3. 「変更」ボタン押下 → 時刻入力 → 「HH:MM に就寝予定」のステータス表示を確認
4. 完全リロード（Ctrl+Shift+R）→ 設定時刻が保持されていることを確認
5. プリセットボタン（22:00, 22:30, 23:00, 23:30, 00:00）タップ → ステータス更新を確認

#### 2026-08-02 追加改修：就寝予定時刻欄の UX ブラッシュアップ

ユーザー手動テストで更なる改善点が指摘された：

1. **未入力時のステータス折り返し問題**：就寝予定未入力時に「未設定（おやすみ画面で入力）」が長く、変更ボタンが 2 行分の高さになっていた
2. **設定就寝時刻の自動デフォルト入力**：設定画面の `targetSleepTime` がデフォルトで今日の就寝予定として反映されていない
3. **プリセットの 5 分刻み化**：プリセット値が固定値（22:00, 22:30, 23:00, 23:30, 00:00）だったが、設定値起点の 5 分刻みの方が実用的

**設計判断（確定）**：

| 項目 | 確定内容 |
|---|---|
| 折り返し修正 | CSS `.brief-wake-sleep-status` に `white-space:nowrap` 追加 + テキストを「未入力」に短縮 |
| デフォルト入力 | `renderTodayBrief` 内で `sleepHabit.sleepPlannedTime[todayKey]` が空の場合、`computeDefaultSleepPlannedTime()` を呼び出してメモリ上のみ自動入力（リロードで再計算、Drive 永続化はユーザーが変更したタイミングのみ） |
| プリセット生成 | `computeSleepPresetTimes` を動的生成に変更：`appSettings.targetSleepTime` を 5 分刻みに丸めた値を起点に、未来 5 個を生成 |

**実装内容**：index.html 26 行変更（追加 21 行 / 削除 5 行、コミット `0a60c1f`）

- `.brief-wake-sleep-status` に `white-space:nowrap` を追加
- 「未設定（おやすみ画面で入力）」→「未入力」に短縮
- `renderTodayBrief` の冒頭にデフォルト入力ロジックを追加
- `computeSleepPresetTimes` を動的生成（`computeWakePresetTimes` のロジックを対称に流用）に変更

**デフォルト入力の動作**：

- `appSettings.targetSleepTime`（設定画面の就寝時刻）が目標時刻
- 目標時刻が未来ならそのまま、目標時刻が過去なら現在時刻を 5 分刻みに切り上げた値を返す
- 既存の `computeDefaultSleepPlannedTime` 関数をそのまま再利用（5561 行目に既存実装あり）

**プリセット生成の動作**：

- `appSettings.targetSleepTime="22:00"` の場合：`["22:00", "22:05", "22:10", "22:15", "22:20"]`
- `appSettings.targetSleepTime="22:07"` の場合：5 分刻み丸めで `["22:05", "22:10", "22:15", "22:20", "22:25"]`
- 24 時を超える場合は `00:00` にラップ（`totalMin >= 24*60` で減算）

#### 2026-08-02 追加改修（寝る前セッション）：設定画面の整理 + 「今日を始める」ボタン再設計

ユーザーFB：「設定tabの起床・就寝予定時刻は、下段の30分刻みのボタンは不要ですね。消してください。HOMEtabの設定画面はかなり良くなりました。一旦今日の筋トレメニューは非表示でお願いします。ツールが完成したらまた表示します。そして、そこに今日を始めるボタンを移動してください。また変更ボタンの仕様ですが、おはようボタンを押してHOMEtabに来たときは、今日を始めるボタンを押すまではタブ遷移できないようにしたいです。(HOMEと設定だけ有効にして、他タブには行けない。)そして、今日を始めるボタンを押したら、その時点で入力されている起床時間と就寝予定時刻をDriveに登録するようにしましょう。」

**【2026-08-03 朝セッション訂正】ユーザーFB訂正**：「おはようボタンは門番画面にそのまま。HOME タブの就寝予定時刻の下部に今日を始めるボタンを設置したい」→ 「今日を始める」ボタンの設置場所を HOME.todaybrief の起床・就寝時刻欄の下部（就寝予定プリセット直下）に修正。設定タブに置いたボタンを削除する形で実装

#### 2026-08-03 午後セッション：UX リデザイン（専用画面 + HOME 編集簡素化）

ユーザーFB：「おはようボタンを押したら、それはアプリの起動をイメージさせることが重要で、そのあとに起床時間が何時だったか？就寝予定時刻は何時か？を確認して今日を始める。」

**新フロー（リデザイン後）**：

1. **門番画面でおはようボタン押下**（サンライズ演出はそのまま）
2. **専用画面（brief-setup）へ遷移**（タブバー非表示）
3. **専用画面で起床・就寝予定時刻を 5 分刻みボタンで簡単入力**
4. **「今日を始める」ボタン押下 → Drive に保存 → HOME.todaybrief へ遷移**
5. **HOME.todaybrief は編集専用画面**（5 分刻みボタンなし、入力 + 変更ボタン）
6. **変更ボタン押下 → Drive 保存 + チェックマーク表示（3 秒）**

**設計判断（確定）**：

| 項目 | 確定内容 |
|---|---|
| 専用画面の存在 | 必須（HOME 画面はスペースが窮屈で 5 分刻みボタンがビジーに見える） |
| タブ遷移制限 | **廃止**（専用画面で入力が確定するため不要） |
| 専用画面のタブバー | **非表示**（入力に集中させる） |
| 専用画面の 5 分刻みボタン | **あり**（HOME よりサクサク決められる） |
| HOME の 5 分刻みボタン | **削除**（直接入力のみ） |
| HOME の変更ボタン | **チェックマーク表示（3 秒）**で保存成功を視覚化 |

**実装内容**：index.html 177 行変更（追加 129 / 削除 48、コミット `924ffb6`）

- 新規 `screen-brief-setup` を HTML に追加
- 新規関数群：`renderBriefSetup`, `setBriefSetupWakeTime/Sleep`, `onBriefSetupWakeTime/Sleep`, `onHomeWakeTime/Sleep`, `confirmHomeWakeTime/Sleep`, `showCheckMark`
- `dismissMorningGate` 内の遷移先を `goTo("brief-setup")` に変更
- `briefStarted` フラグと goTo の遷移制限ロジックを完全削除
- `tabbar` 表示制御を拡張：`(name==="morninggate" || name==="brief-setup") ? "none" : ""`
- HOME.todaybrief から 5 分刻みプリセットボタンを削除、input + 変更ボタンに統一
- 変更ボタン押下時に「✓」チェックマークを 3 秒間表示（`showCheckMark` 関数）

**新フローの動作**：

- 門番画面 → おはようボタン押下 → サンライズ演出 → brief-setup 画面（タブバー非表示）
- brief-setup で起床時刻（5 分刻み過去プリセット）と就寝予定時刻（5 分刻み未来プリセット）を編集
- 「今日を始める」押下 → `startBrief()` 実行 → 起床・就寝時刻を Drive に保存 → `goTo("todaybrief")` で HOME へ遷移
- HOME.todaybrief に着地すると、各タブが利用可能（タブ遷移制限なし）

**変更ボタンの UX**：

- 通常の表示：「変更」
- 押下後 3 秒間：「✓」（disabled）
- 3 秒後に元に戻る
- onchange でメモリ上のみ更新（即座にステータス表示に反映）、変更ボタン押下で Drive に保存

#### 2026-08-03 夕方セッション：brief-setup 画面の UI 改善

ユーザーFB：「起床時刻と就寝予定時刻の入力欄が小さいので、起床時刻というテキストの右側に大きく入力項目を設置できますか？また5分刻みのボタンは横スクロールでもっと多くのボタンを選択できるようにできますか？また起床時刻と就寝予定時刻の間にもっとスペースを空けて見やすくしてください。ボタンの下部のテキストは縦にしないと見えづらいです。」

**改善内容**：

| 項目 | 改善 |
|---|---|
| 入力欄 | label（☀️ 起床 / 🛏️ 就寝）+ input を横並びに。input を大きく（font-size:22px, padding:12px 16px, flex:1） |
| 5 分刻みプリセット | 横スクロール対応（`overflow-x:auto`, `white-space:nowrap`）。数を 5 → 12 個に増加（過去 1 時間/前後 30 分幅） |
| 起床・就寝時刻の間 | `.brief-setup-section-gap` で `margin-top:38px` |
| hint テキスト | `.brief-setup-hint` で 1 行化（`white-space:nowrap`）+ font-size:12px |

**実装内容**：index.html 34 行変更（追加 24 / 削除 10、コミット `215f9b2`）

- 新規 CSS：`.brief-setup-input-row`, `.brief-setup-input-row label`, `.brief-setup-input-row input[type="time"]`, `.time-preset-row-scrollable`, `.brief-setup-section-gap`, `.brief-setup-hint`
- HTML 構造変更：`<label>` を input の前に追加（label + input 横並び）
- プリセットボタン数を増やす：`computeWakePresetTimes` を 5 → 12 個（過去 11 + 現在 1）、`computeSleepPresetTimes` を 5 → 12 個（過去 15 分 ～ 未来 45 分）
- 「今日を始める」ボタンの `margin-top` を 30 → 36px に拡張

#### 2026-08-03 夕方セッション第 2 段：brief-setup 画面の最終 UI 改善

ユーザーFB：「起床時刻と就寝予定時刻のテキスト表示はもう少し分かりやすい表示にできないですか？太陽アイコンの右に起床という文字があるので、これは消してもいいと思います。その代わり上部の起床時刻テキストはもう少し目立つようにしましょう。アイコンの場所とバランス見てください。今日を始めるボタンはもっと下の方に設置できますか？」

**改善内容**：

| 項目 | 改善 |
|---|---|
| label | 完全削除（☀️ 起床 / 🛏️ 就寝 のテキスト重複を解消、input のみに） |
| section-label | brief-setup 専用スタイルで強化（font-size:16px, color:var(--ink), font-weight:700, text-transform:none） |
| section-label 間隔 | `margin:28px 0 10px` → `margin:32px 0 14px` |
| brief-setup-section-gap | 38px → 48px |
| 「今日を始める」ボタン | `.brief-setup-cta-wrap` で囲み `margin-top:64px` で画面下部に移動。ボタン自体も大きく（padding:14px 20px, font-size:15px） |

**実装内容**：index.html 14 行変更（追加 10 / 削除 4、コミット `1832e24`）

- HTML から `<label>` を完全削除
- 新規 CSS：`#screen-brief-setup .section-label`（専用上書き）、`.brief-setup-cta-wrap`
- 「今日を始める」ボタンを `.brief-setup-cta-wrap` で囲み、margin-top:64px で下に押し下げ

#### 2026-08-03 夕方セッション第 3 段：HOME.todaybrief レンダリング漏れ修正

ユーザーFB（スクショ報告）：「専用画面で今日を始めるボタンを押したら、HOMEタブが表示されますが、今日を始めるボタン以外が表示されていません。」

**原因**：`dismissMorningGate` で `goTo("brief-setup")` に変更した結果、`renderTodayBrief()` が呼ばれなくなり、HOME.todaybrief の `brief-wake-sleep` 要素の中身（起床・就寝時刻の入力欄）がレンダリングされない問題が発生。

また、HOME.todaybrief には不要な「今日を始める」ボタンも残っていた。

**修正内容**：

| 項目 | 修正 |
|---|---|
| `startBrief` 関数 | `goTo("todaybrief")` の前に `renderTodayBrief()` を呼ぶ |
| HOME.todaybrief HTML | 不要な「今日を始める」ボタン（id="brief-cta"）を削除（専用画面で押すので不要） |

**実装内容**：index.html 8 行変更（追加 3 / 削除 5、コミット `acd1458`）

#### 2026-08-03 夕方セッション第 4 段：変更ボタンの成功状態改善 + ステータス更新タイミング修正

ユーザーFB：「変更ボタンを押した後のボタンは背景色を緑にしてください。変更ボタンの右側に21:25に起床済みというテキストがありますが、起床時刻の入力欄を変更しただけでテキストの値が変更されています。このテキストの値は変更ボタンを押したら変更されるようにしたいです。」

**改善内容**：

| 項目 | 改善 |
|---|---|
| 変更ボタンの成功状態 | ✓ チェックマーク + **背景色を `--healthcare`（緑系）に**。`.time-preset-btn.success-check` クラスを 3 秒間付与 |
| ステータス更新タイミング | **変更ボタン押下時まで遅延**。onchange ではメモリ更新のみ。confirmHomeWakeTimeEdit / confirmHomeSleepTimeEdit で「HH:MM に起床済み」「HH:MM に就寝予定」を更新 |

**実装内容**：index.html 23 行変更（追加 14 / 削除 9、コミット `df1f4fa`）

- 新規 CSS：`.time-preset-btn.success-check{background:var(--healthcare); color:#fff; border-color:var(--healthcare); font-weight:700;}`
- `showCheckMark` 関数で `success-check` クラスを 3 秒間付与・削除
- `onHomeWakeTimeChange` / `onHomeSleepTimeChange` からステータス更新ロジック削除（メモリ更新のみ）
- `confirmHomeWakeTimeEdit` / `confirmHomeSleepTimeEdit` でステータス表示を更新

#### 2026-08-03 夜セッション：HOME から不要セクション削除 + リサーチタスク一覧追加

ユーザーFB：「HOMEタブの通勤中にチェックしたいコンテンツとジャーナルのヒントって、生成ロジックって何も検討できてないですかね？であればHOMEタブから消してもいいかなと思います。通勤中にはリサーチタスクを消化したくなると思うので、リサーチタスクの一覧を表示してくれれば良いかなと思います。」

**変更内容**：

| 項目 | 変更 |
|---|---|
| 「通勤中にチェックしたいコンテンツ」セクション | 削除（生成ロジック未検討のため） |
| 「ジャーナルのヒント」セクション | 削除（生成ロジック未検討のため） |
| 「リサーチタスク」セクション | 新規追加（通勤中に消化したくなる research タスクを表示） |
| `getJournalHints` 関数 | 削除（renderTodayBrief からのみ参照） |
| `getCommuteRecommendations` 関数 | 削除（renderTodayBrief からのみ参照） |
| `getResearchTasksForBrief` 関数 | 新規追加（open な research タスクを priority 昇順 → due 昇順でソート） |

**リサーチタスク一覧の仕様**：

- `taskData` から `type === "research" && status === "open" && !isBlocked(t)` でフィルタ
- ソート：priority 昇順 → due 昇順
- 表示：上位 5 件、📚 アイコン + タイトル + due 日のバッジ
- リサーチタスクが 0 件の場合：「リサーチタスクはまだありません」メッセージ表示

**実装内容**：index.html 63 行変更（追加 23 / 削除 40、コミット `39ca99d`）

**設計判断（確定）**：

| 項目 | 確定内容 |
|---|---|
| 設定タブの 30 分刻みボタン | 削除（時刻は input[type="time"] で直接入力） |
| HOMEtab 筋トレメニュー | 非表示（HTML + renderTodayBrief 内描画ロジック両方コメントアウト） |
| 「今日を始める」ボタン位置 | HOME.todaybrief の起床・就寝時刻欄の下部（就寝予定時刻のプリセットボタン直下） |
| タブ遷移制限 | おはようボタン押下後 → 「今日を始める」押下まで HOME と設定タブのみ遷移可能 |
| 起床/就寝時刻の Drive 登録タイミング | 「今日を始める」ボタン押下時に `persistHabitChange` で Drive に保存 |

**タブ遷移制限の実装**：

- 新規フラグ `briefStarted` を追加（`hasGreetedToday` と独立管理）
- `dismissMorningGate` で `briefStarted = false` をセット
- `goTo` 関数の冒頭で遷移制限チェック：`briefStarted === false` かつ `name` が "todaybrief" / "settings" / "morninggate" / "goodnight" 以外なら `return` でブロック
- 起床時刻・就寝予定時刻は「設定タブで変更可能」「おはようボタン押下後は初期表示のみ」のため、ユーザーが変更してから Drive に保存する設計

**「今日を始める」ボタンの実装**：

- 新規関数 `startBrief` を `setSleepPlannedPreset` の直後に追加
- `async function` で `persistHabitChange` を await
- 起床時刻（`wakeHabit.wakeTime[todayKey]`）と就寝予定時刻（`sleepHabit.sleepPlannedTime[todayKey]`）が存在する場合のみ Drive に保存
- `briefStarted = true` でタブ遷移制限を解除
- `goTo("todaybrief")` で HOME 画面に戻る

**Drive 未接続時の挙動**：

- `persistHabitChange` 関数が `googleAccessToken` 未設定時は警告ログのみで正常 return（既存パターン、Task 73 で実装済み）
- したがって未接続状態でもエラーにならず、メモリ上の変更は保持される（リロードで消える仕様）

#### 2026-08-03 夜セッション第 2 段：HOME 整理（入力欄位置 + リサーチタスクタップ）

ユーザーFB：「HOME画面を少し整理したいです。起床時刻と就寝予定時刻の入力欄の開始位置はそろえてください。リサーチタスクはタップしたらタスク詳細画面を表示したいです。」

**変更内容**：

| 項目 | 変更 |
|---|---|
| 起床/就寝時刻入力欄の開始位置 | `.brief-wake-sleep-label` に `#brief-wake-sleep` スコープで `min-width:90px` を追加し、input 開始位置を揃える |
| リサーチタスク一覧のタップ | `task-compact-row` に `onclick="openTaskDetail('${t.id}')"` + `cursor:pointer` を追加 |

**設計判断**：

| 項目 | 確定内容 |
|---|---|
| 最小幅のスコープ | `#brief-wake-sleep` 限定（goodnight 画面の「🛏️ 今夜」ラベルには影響させないため） |
| 最小幅の値 | 90px（`☀️ 起床` 4 文字 + 絵文字分の余白を考慮） |
| 「気になっているTask」セクションのタップ | 依頼に含まれていないため未対応。リサーチタスクと仕様が違い、要不要を別途確認したい |
| リサーチタスクタップの遷移先 | 既存の `openTaskDetail(id)`（Tasks タブ 4625 行目と同パターン） |

**実装内容**：index.html 4 行変更（追加 3 / 削除 1、コミット `0964a56`）

**注意点**：

- コミット分割ルール（CLAUDE.md）に違反し、2 つの変更が 1 コミット（`0964a56`）にまとまった。`git add` 時点で両方含まれていたため。次回は離れた箇所の複数 Edit をする場合、Edit → commit を 1 単位ずつ繰り返す


**実装内容**：index.html 99 行変更（追加 63 / 削除 36、コミット `4785ee1`）

#### 2026-08-03 夜セッション第 3 段：HOME 整理 + タスク詳細画面改善

ユーザーFB：
- 「HOMEタブの変更ボタンの横幅が小さすぎて、変更の文字が縦に並んでいます。文字は必ず横に並ぶようにしてください。このデザインルールは今後も適用してください。」
- 「リサーチタスクのとなりの通勤中タグは消してください。」
- 「気になっているタスク(TASKはカタカナにして下さい)もタップで詳細を開くようにして下さい。」
- 「タスク詳細は 本文はもっと縦幅を大きくしたいです。先行タスクは一旦非表示にしてください。完了ボタンを設置してください。」

**変更内容**：

| 項目 | 変更 |
|---|---|
| `.time-preset-btn` 共通 CSS | `white-space:nowrap` + `min-width:60px` を追加（変更ボタン等の文字が縦に折り返される問題を修正） |
| 「リサーチタスク」セクションの通勤中バッジ | 削除 |
| 「気になっているTask」セクション | 「気になっているTASK」にリネーム + タップ可能化（`onclick="openTaskDetail('${t.id}')"` + `cursor:pointer`） |
| 0件メッセージの表記 | 「今日は特に気になるTASKはありません」に統一 |
| タスク詳細画面の本文 textarea | `min-height: 80px` → `200px` に拡大 |
| タスク詳細画面の先行タスク関連 | 一旦非表示（HTML コメントアウト） |
| タスク詳細画面の完了ボタン | 新設。`renderTaskActionArea` / `completeTaskFromDetail` を新規追加 |

**設計判断**：

| 項目 | 確定内容 |
|---|---|
| ボタンの文字横並び保証 | CSS レベル：`white-space:nowrap` + `min-width:60px`（`time-preset-btn` 全般） |
| デザインルール | CLAUDE.md に「ボタン内の文字は必ず横に並ぶ（縦書き禁止）」を追記。`min-width: 60px` を基本形に |
| 「気になっているTask」セクションのタップ | リサーチタスクと同じ扱い（`openTaskDetail` 呼び出し） |
| 完了ボタンの遷移先 | HOME（todaybrief）に戻る。`renderTodayBrief` を呼んで brief-tasks / brief-research-tasks を再描画 |
| 完了済みタスクの表示 | 「✅ 完了済み」テキスト表示（取り消し不可、要望に応じて表示のみ） |
| 先行タスク機能の扱い | 一旦非表示（HTML コメントアウト）。`renderTaskDeps` 等の関数とコードは残置。仕様再検討後に復活予定 |

**新規関数の動作**：

- `renderTaskActionArea`：
  - `currentTaskDetailId` から該当タスクを取得
  - `status === "done"` → 「✅ 完了済み」表示
  - `status === "open"` → 「✅ 完了にする」ボタン（primary）を描画
- `completeTaskFromDetail`：
  - `toggleTaskDone` と同じ順序で処理（メモリ更新 → Drive 永続化）
  - Drive 未接続時は警告ログのみ
  - 完了後に `renderTodayBrief` + `goTo("todaybrief")` で HOME に戻る

**コミット分割（理想通り）**：

- `1b07dc1` refactor(HOME/task-detail): 変更ボタン幅修正 + 通勤中バッジ削除 + TASK表記・タップ対応 + タスク詳細画面改善
- `6dbc565` docs(CLAUDE.md): デザインルールに「ボタン内文字は必ず横に並ぶ」を追加

注：1 つ目のコミットには複数の機能変更が含まれているが、index.html 内の全 UI 変更として 1 つの論理的なまとまり（HOMEtask 整理）として扱う。CLAUDE.md とは別コミットに分離した。

#### 関連節

- 2.11：Habit のスキーマと基本仕様（既存）
- 2.12：特別な習慣（wake / sleep / weight_training / steps）の仕様（既存）
- 4.4.4：Habit wake/sleep の Drive 永続化（既存、`doGreetAction` 経由）
- 4.4.5：通常 Habit の check_time 切替 Drive 永続化（既存、`setHabitCheckTime` 経由）
- 4.4：camelizeKeys ヘルパー（既存、メモリ上キャメル / YAML 上スネーク）
- 4.7：ロードマップ（既存、Task 完了 UI の将来構想あり）

#### 2026-08-03 夜セッション第 4 段：完了ボタンのUI改善

ユーザーFB：「完了ボタンのUIは、完了にするボタンの背景は未押下の時は無色にして、押下したら緑にしましょう。(チェックマークアイコンは消してＯＫ) 緑になって1秒で遷移前の画面に戻るようにしましょう。」

**変更内容**：

| 項目 | 変更 |
|---|---|
| 完了ボタンのラベル | 「✅ 完了にする」→「完了にする」（チェックマーク削除） |
| 押下前のスタイル | 無色（背景透明・枠線のみ） |
| 押下後のスタイル | 緑背景（`var(--healthcare)`）+ 白文字 + 太字（1秒間表示） |
| 遷移タイミング | 押下から 1秒後に HOME（todaybrief）へ |
| 連続押下防止 | `disabled` 化で対処 |

**新規 CSS クラス `.btn-complete`**：

```css
.btn-complete{background:transparent; color:var(--ink-dim); border:1px solid var(--border); padding:9px 16px; border-radius:14px; font-size:12.5px; min-width:120px; cursor:pointer; transition:all 0.2s; font-family:inherit;}
.btn-complete:hover{background:var(--surface2); border-color:var(--primary); color:var(--ink);}
.btn-complete:active{transform:scale(0.98);}
.btn-complete:disabled{cursor:default;}
.btn-complete.success{background:var(--healthcare); color:#fff; border-color:var(--healthcare); font-weight:700;}
```

**`completeTaskFromDetail` 関数の処理順序**：

1. ボタンの `disabled` 化（連続押下防止）
2. ボタンに `success` クラスを追加（緑背景化）
3. ボタンラベルを「完了」に変更
4. メモリ上の `t.status = "done"` を即座に反映
5. `setTimeout(1000)` 開始
6. 1秒後：
   - Drive 永続化（`updateTaskStatusInDrive(t.id, "done")`）
   - `renderTodayBrief()` で HOME の brief-tasks / brief-research-tasks を再描画
   - `goTo("todaybrief")` で HOME 画面へ遷移

**設計判断**：

| 項目 | 確定内容 |
|---|---|
| 押下前の「無色」 | 背景 = `transparent`、枠線 = `var(--border)`、文字 = `var(--ink-dim)`。押せることは認識できるが無彩色で「強調しない」 |
| 押下後の「緑」 | 既存の `.time-preset-btn.success-check` と同じ `var(--healthcare)` で統一感 |
| 1秒間の意図 | 緑のフィードバックを視認してから遷移する自然な UX。短すぎると気づかない、長すぎると待たされる |
| 連続押下防止 | ボタンの `disabled` 属性で対処（処理中フラグも兼ねる） |
| Drive 永続化のタイミング | 遷移前の 1秒間 setTimeout 内。非同期で実行されるため UX を妨げない |
| メモリ更新の即時性 | 押下直後に `t.status = "done"` を反映。HOME に戻った時点でリストから消える |

**CLAUDE.md デザインルール追加**：

- 「取り消しの大きい操作（タスク完了等）の成功状態は 1秒間緑背景で表示してから遷移する」
- 今後も `btn-complete` パターンを他機能（習慣完了、Knowledge 完了など）でも使い回せる

**コミット分割（理想通り）**：

- `4dc702e` refactor(task-detail): 完了ボタンを無色→押下後緑+1秒で HOME 遷移
- `a18fd6d` docs(CLAUDE.md): デザインルールに「完了ボタン成功状態（1秒緑→遷移）」を追加

#### 2026-08-04 セッション：削除確認ポップアップをアプリ内モーダルに置き換え

ユーザーFB：「削除時のポップアップはブラウザ側で実行していると思いますが、これはアプリ内でポップアップできないですか？」

**変更内容**：

| 項目 | 変更 |
|---|---|
| 削除確認 UI | ブラウザネイティブ `confirm()` → アプリ内モーダル（`.modal-backdrop` + `.modal-card`） |
| 新規 CSS | `.btn-danger`（赤系 #e0716e、削除確認用）, `.modal-backdrop`, `.modal-card`, `.modal-title`, `.modal-message`, `.modal-actions` |
| 新規 JS 関数 | `openConfirmModal({ title, message, confirmLabel, cancelLabel, danger, onConfirm, onCancel })` |
| 閉じる方法 | 背景タップ、ESC キー、キャンセルボタンの 3 通り |
| フォーカス管理 | 開いた時に確認ボタンにフォーカス、閉じた後に元の要素にフォーカスを戻す |

**`openConfirmModal` の API**：

```js
openConfirmModal({
  title: "タスクを削除",                  // 省略可
  message: "「タスク名」を本当に削除しますか？\n\nこの操作は元に戻せません。",
  confirmLabel: "削除する",               // 省略時 "OK"
  cancelLabel: "キャンセル",              // 省略時 "キャンセル"
  danger: true,                            // true で確認ボタンが .btn-danger（赤系）
  onConfirm: () => { ... },                // 確認時に呼ばれる
  onCancel: () => { ... }                  // キャンセル時（背景・ESC・キャンセルボタン）
});
```

**削除確認モーダルの表示仕様**：

- タイトル：「タスクを削除」
- メッセージ：「『タスク名』を本当に削除しますか？\n\nこの操作は元に戻せません。」
- 確認ボタンラベル：「削除する」（`.btn-danger` で赤系）
- キャンセルボタンラベル：「キャンセル」（`.btn-secondary`）

**設計判断**：

| 項目 | 確定内容 |
|---|---|
| 共通化 | `openConfirmModal` を共通関数として実装。今後他の confirm 呼び出し（記事取得失敗時、下書き削除等）も移行可能 |
| ブラウザ alert/confirm の扱い | スコープ外として残置。エラー通知は alert のまま。破壊的確認（confirm）は順次モーダルに移行予定 |
| ESC キーの挙動 | グローバル keydown リスナーで `Escape` 押下時 → onCancel。モーダル閉時に必ず removeEventListener |
| フォーカストラップ | 今回は未実装（Tab キーでフォーカス移動するが、モーダル外には抜けられる）。必要なら Tab トラップを後追加 |
| アニメーション | バックドロップはフェードイン、モーダルカードはポップイン（0.2s）。CSS アニメーションで実装 |
| z-index | 1000 で最前面。`.tabbar`（z-index: 10）よりも上 |

**CLAUDE.md デザインルール追加**：

- 「確認ポップアップはアプリ内モーダルで実装する」
- ブラウザネイティブの `confirm()` / `alert()` は使用禁止
- 共通関数 `openConfirmModal` の使用を推奨
- 破壊的操作は `danger: true` で `.btn-danger` を使う

---

### 2.11 習慣（Habit）— 新設・毎日チェックする本命機能

**位置づけ**：Great Mindが「たまに参照する背景の知恵」であるのに対し、Habitは**毎日チェックする、頻繁に触る本命の機能**。ユーザーが本当に管理したかったのはこちら。

**具体例**：「5時半に起きる」「22時に寝る」「1日1回は体を動かす」のような、毎日繰り返す行動。

**朝チェック／夜チェックの選択**：Habitごとに、チェックを促すタイミングが「朝」か「夜」かを設定できる（`check_time`）。例えばウェイトトレーニングを朝やる人・夜やる人がいるため、ユーザーごとに選べるようにする。

**チェックの仕組み**：
- 登録した各Habitについて、`check_time`に応じたタイミングで「今日はできましたか？」とYes/Noで確認する
- UI操作は、Taskの「見直しタイム」と同じ**フリック（スワイプ）＋外側タップ**の仕組みを流用する（左＝できなかった、右＝できた、など）
- 1日1件ずつではなく、その日まだ未回答のHabitをカードスタックのように順番に確認していく

**継続日数（ストリーク）**：
- Yesが連続した日数を「ストリーク」として計算する
- アプリを開いた時（特に朝）に「〇〇日連続達成中」を表示し、モチベーションにする
- カレンダー表示で、日ごとの達成・未達成を一覧できるようにする

**Great Mindとの関係**：
- Habitの実行記録そのものが、間接的にGreat Mindのalignmentのエビデンスにもなりうる（例：「22時に寝る」を継続できていることは、関連するGreat Mindへの`agree`の裏付けになる）。ただしこれは自動連携の将来課題とし、今はHabit自体の記録に専念する

**スキーマ（通常のHabit。独立ファイル：`09_Habit/{id}.md`）**
```yaml
type: habit
id: "..."
schema_version: 1
title: "（習慣。例：1日1回は体を動かす）"
themes: ["healthcare"]
check_time: "morning" | "night"     # 朝チェックか夜チェックか
special: null
related_notes: []                   # ジャーナルや記事でこの習慣を発見した場合、そのNoteのid（Taskと同じ考え方）
created: ...
status: "active" | "archived"
log:
  "2026-07-08": true
  "2026-07-09": false
```

### 2.12 特別な習慣（起床・就寝・コアトレーニング）

起床・就寝・コアトレーニング（ウェイトトレーニング／歩数）は、他のHabitと違い**自動判定またはアプリ内の専用アクションと連動する**、基本的な習慣として特別枠で扱う。

**① 起床（special: "wake"）**
- `target_wake_time`（例：05:30）を設定する
- Capture画面に常設された「☀️ おはよう」ボタンを、設定時刻から**30分以内**にタップすると、その日は自動的にクリア（`log[today] = true`）になる
- 30分を過ぎてから押した場合、または押さなかった場合は未達成

**② 就寝（special: "sleep"）**
- `target_sleep_time`（例：22:00）を設定する
- Capture画面に常設された「🌙 おやすみ」ボタンを、設定時刻の**30分前〜60分前の間**にタップすると、その日はクリアになる（スクリーンタイムを避けるため、その時間帯より前に区切りをつける意図）
- ウィンドウ外にタップした場合は未達成

**③ コアトレーニング（special: "weight_training"）**
- ウェイトトレーニングの実施。`check_time`（朝／夜）はユーザーが選択（朝にやる人・夜にやる人がいるため）
- 判定は通常のHabitと同様、Yes/Noのフリックチェックで行う（自動判定ではない）

**④ 歩数（special: "steps"）**
- 1日の目標歩数（`step_goal`。デフォルト10000）に対する実績を記録する
- Yes/No形式ではなく、**歩数そのものを毎日入力する**数値ログ
- 7日平均・14日平均・28日平均を算出し、長期的に目標歩数に届いているかを確認する
- 直近14日間は、日ごとの実績も一覧できるようにする
- **自動連携（Apple HealthKit / MyFitnessPal API）は、Webアプリ（PWA）の技術的制約により現時点では組み込めない**（HealthKitはネイティブiOSアプリ専用でWebからは一切アクセス不可。MyFitnessPal APIも非公開・新規受付停止中）。当面は手動入力とし、将来的にiOSショートカット経由での間接連携を検討する

**スキーマ（特別なHabit）**
```yaml
type: habit
id: "..."
title: "起床" | "就寝" | "ウェイトトレーニング" | "歩数"
themes: ["healthcare"]
special: "wake" | "sleep" | "weight_training" | "steps"
check_time: "morning" | "night"        # weight_trainingのみ使用
target_wake_time: "05:30"              # special:"wake"のみ
target_sleep_time: "22:00"             # special:"sleep"のみ
step_goal: 10000                       # special:"steps"のみ
step_log:                              # special:"steps"のみ。歩数の数値ログ
  "2026-07-08": 9500
log:                                   # wake/sleep/weight_trainingで使用。Yes/No判定結果
  "2026-07-08": true
created: ...
status: "active"
```

### 2.13 Settings（設定）

ユーザーが各種タイミング・目標値を設定する画面。新しくフッターに「設定」タブを持つ。

```yaml
type: settings
task_review_morning_time: "08:00"    # これ以降にアプリを開くと、Taskの見直しタイムが朝の分として表示される
task_review_night_time: "20:00"      # これ以降にアプリを開くと、Taskの見直しタイムが夜の分として表示される
```

Task側にも「朝チェック／夜チェック」の概念を持たせるかは今後の検討事項（現状のTaskの「見直しタイム」は時刻に関わらず放置日数だけで判定しているが、設定した時刻以降のみ表示する、という制御にすることも検討できる）。

---

## 3. スキル・アセットツリー仕様

### 3.1 構造
中心ルート「スキル・アセット」から、右にスキル系、左にアセット系を伸ばす双方向ツリー。

- **右（Skill系）**：ツール活用（AI系／IT系など）、事業運営スキル（営業・提案など）
- **左（Asset系）**：専門知識・フレームワーク、事例・実績

### 3.2 階層の安定度ルール
| 階層 | 内容 | 誰が決めるか | 変更方針 |
|---|---|---|---|
| Lv1 | ルート | ユーザー | 固定 |
| Lv2 | カテゴリ | ユーザー | 固定 |
| Lv3 | ツール／知識名 | Claudeが気づけば提案 | 低頻度・慎重に、提案ベース |
| Lv4以降 | トピック | Claude | 随時、自由に再構成 |

### 3.3 ノートの性質（追記・更新型）
1ツール・1知識につき1ノートを継続的に育てる。使うたびに追記・更新し、末尾に簡単な変更履歴を積み上げる。ジャーナルやソースのような「スナップショット型」とは異なる。

### 3.4 理解度のグラデーション
各トピックに `maturity` を持たせる。
- **mature（完成）**：しっかり書き込まれている。濃く表示
- **developing（育成中）**：中間の濃さ
- **stub（要調査）**：ジャーナル等の気づきから生まれた「まだ手をつけていない」項目。薄く表示し、「要調査」バッジを付ける

### 3.5 将来構想（今は作り込まないが、データだけ揃えておく）
- Mind側の「悩み・課題」アトムには `concern` の印を付ける
- Skill/Asset側のノートには「何を解決するか（solves）」のキーワードを付ける
- 将来、この2つをマッチングして「この悩みはこのナレッジで解決できるかも」と提案する機能につなげる

---

## 4. Task（タスク）

Knowledgeとは別の、独立したオブジェクトとして扱う。

### Action Task
現実世界で実行する行動。例：ビジョンマップを作る、社長へ退職を報告する。

### Research Task
Knowledgeの精度向上のための調査タスク。以下の場合に自動生成する。
- confidenceが低い
- 根拠・ソースが不足している
- 仮説中心の内容である

### 4.1 管理方式：期日ベースではなく「優先順位＋完了フラグ＋依存関係」

Taskは基本的に**期日（due date）で管理しない**。代わりに以下の3つで管理する。

1. **優先順位（priority）**：数値が小さいほど優先度が高い。同じテーマ内での相対的な順位づけに使う
2. **完了フラグ（status）**：`open`か`done`かだけを持つ。いつまでにやるかではなく、やったかどうかだけを記録する
3. **依存関係（depends_on）**：そのタスクに着手する前に終わらせておくべき「先行タスク」のIDを配列で持つ。後続タスクは別フィールドを持たず、他のタスクの`depends_on`を検索して逆算する（Great Mindのalignmentと同じ考え方で、関係は片方向にだけ保存し、逆方向は都度計算する）

**期日を完全に禁止するわけではない**：明確に「この日までにやる必要がある」タスク（例：予約の締切）には`due`を任意で残せるが、基本方針はあくまで優先順位ベースとする。

### 4.2 テーマ分類

Taskにも既存の4テーマ分類（Healthcare/Business/Mind/Relationships）を適用する。基本的には親Noteのthemesを継承するが、Task単体で別のthemesを持たせても良い（Knowledgeと同じ考え方）。UI上ではテーマごとにタスク一覧を分けて表示できるようにする。

### 4.3 スキーマ（独立ファイル。`07_Task/{id}.md`）
```yaml
id: "..."
schema_version: 1
type: "action" | "research"
text: "（タスクのタイトル。短い1文）"
body: "（任意。詳細・メモ。ユーザーが自由に編集できる）"
themes: ["business"]              # 親Noteから継承、または個別指定
priority: 1                        # 数値が小さいほど優先度が高い。テーマ内での相対順位
status: "open" | "done" | "someday"  # someday = 見直しの結果、一旦保留にしたタスク（アーカイブ）
depends_on: ["task_id", "..."]     # 先行タスク（同じ07_Task/フォルダ内のid、任意・複数可）
due: "（任意。明確な締切がある場合のみ）"
created: "（作成日）"
last_confirmed: "（直近で見直しが行われた日。nullなら未見直し）"
archived_at: "（statusがsomedayになった日。復活したらnullに戻す）"
origin_note: "（生まれたNoteのid）"
```
親Note側には`task_refs: [id, ...]`という軽量な参照リストのみを持たせる（詳細は0.5節）。

### 4.4 放置タスクの見直しと、保留タスクのアーカイブ・復活

**見直し（stale review）**：`created`（または`last_confirmed`）から一定期間（目安5日）動きがないopenタスクは、「まだ重要か」をユーザーに確認する対象になる。UIではスワイプ（左＝保留／右＝まだ重要）または明示的なボタンで回答する。

- **「まだ重要」**：`last_confirmed`を今日の日付に更新するだけ。優先順位は変えない。次の見直しサイクルまで再度は聞かれない
- **「一旦保留」**：`status`を`someday`に変更し、`archived_at`に日付を記録する。通常のタスク一覧（テーマ別・クロステーマ）からは除外され、アーカイブとして保持される（削除はしない）

**復活（revival）**：新しいJournal/Sourceを処理する際、その内容がアーカイブ（`status: someday`）されているタスクと意味的に近いと判断した場合、そのタスクを復活させる。
1. `status`を`open`に戻す
2. `archived_at`をnullに戻す
3. `last_confirmed`を今日に更新
4. **優先順位を、他のタスクより少しだけ高く設定する**（例：そのテーマの現在の最小priority値より1つ小さい値にする）。これは「何度も気になっている・繰り返し出てくる関心事」であることの表れなので、通常の新規タスクより優先度を上げて扱う

この一連の判断（アーカイブとの類似性の検出、復活の実行）はClaudeが新規入力の処理時に行う。ユーザーからは「前に保留にした件、また気になってるんですね」と自然に見える形になるのが理想。

---

### 4.4.1 完了操作（2026-07-23 実装）

タスクカードのチェックボックスをタップすると、`status` を `open` ⇔ `done` で切り替える。完了済みタスクは一覧の上位表示から自動的に「もっと見る」アコーディオン内へ退避する（既存 UI の挙動をそのまま利用）。

**Drive 永続化の設計判断（経緯）**：

2026-07-23 時点で、UI（チェックボックス）と状態遷移ロジック（`toggleTaskDone` 関数）は既に実装されていたが、メモリ上の状態変更のみであり Drive への書き出しは行われていなかった（リロードすると元に戻る）。これは「Capture以外からの書き込み」全体が未着手だったことに起因する。

まず最小実装として、Task完了操作のみ Drive 永続化を実装した。

**実装方針**：
- 既存 UI（チェックボックス）をそのまま利用。UI刷新は行わない
- `toggleTaskDone(id)` を async 化
- メモリ上の `status` を即座に更新 → UI 再描画 → バックグラウンドで Drive へ書き込む（楽観的更新）
- Drive 上の原本ファイル（`07_Task/{id}.md`）を取得し、frontmatter 全体の `status` フィールドのみ書き換えて書き戻す方式。これにより `last_confirmed` / `archived_at` / `origin_note` などの他フィールドが失われない
- `createOrUpdateFile()`（既存、2054行目）を利用。同名ファイルが Drive 上に存在すれば PATCH、なければ POST
- 失敗時は alert で警告。メモリ状態はロールバックせず、リロードまで変更は保持される（リロード後は Drive 上の正しい状態に戻る）
- Drive 未接続時は警告ログのみ。モック配列での動作確認は可能

**未着手の関連操作**（今後の予定）：
- タイトル変更（`saveTaskTitle`）、本文変更（`saveTaskBody`）、Due date 変更（`saveDueDate` / `clearDueDate`）、依存タスク追加・削除（`addDep` / `removeDep`）も同様にメモリ上のみ
- これらも同じパターン（楽観的更新 + Drive 書き戻し）で実装可能

### 4.4.2 編集系操作のDrive永続化（2026-07-24 実装）

4.4.1 で実装した `updateTaskInDrive` を汎用関数として確立し、編集系の操作も Drive 永続化した。

**実装した操作**：
- `saveTaskTitle()`：タスク詳細画面のタイトル編集時に `{ text }` を Drive に書き戻し
- `saveTaskBody()`：本文編集時に `{ body }` を書き戻し
- `saveDueDate()` / `clearDueDate()`：Due date 設定・解除時に `{ due }` を書き戻し
- `addDep()` / `removeDep()`：依存タスクの追加・削除時に `{ depends_on }` を書き戻し（フロントの `dependsOn` とは key 名が異なるので注意）

**実装パターン**（確立）：

```javascript
async function updateTaskInDrive(taskId, updates){
  // ① Drive上の原本 frontmatter を取得
  // ② Object.assign(fm, updates) で部分更新
  // ③ buildFrontmatterFile で再構築 → createOrUpdateFile で書き戻し
}

async function persistTaskChange(taskId, updates){
  // ① Drive接続チェック（未接続なら警告ログのみ）
  // ② updateTaskInDrive を呼ぶ
  // ③ エラーは console.error（トーストUI等は将来課題）
}
```

**注意点**：
- フロントの `taskData` プロパティ名（camelCase）と frontmatter のキー名（snake_case）が異なる場合がある。`dependsOn` → `depends_on` のように、写像が必要
- `toggleTaskDone` だけは既存仕様（alert でエラーを通知）を維持するため、`persistTaskChange` は使わず `updateTaskStatusInDrive` を直接呼んでいる
- 編集系の操作はユーザの入力のたび（oninput/onchange）に呼ばれるため、連続呼び出しでの API レート制限に注意。実用上は問題ないが、将来デバウンスを検討

### 4.4.3 見直し・復活の Drive 永続化（2026-07-28 実装）

4.4.1〜4.4.2 で確立した `persistTaskChange` パターンを、`staleDecide`（見直し）と `reviveTask`（復活）に適用した。

**実装した操作**：
- `staleDecide(id, stillImportant=true)`：スワイプ右／「キープする」ボタン。`lastConfirmed` を今日に更新
- `staleDecide(id, stillImportant=false)`：スワイプ左／「アーカイブ」ボタン。`status` を `someday` に、`archivedAt` を今日に、`lastConfirmed` を今日に更新
- `reviveTask(id)`：アーカイブから復活。`status` を `open` に戻し、`archivedAt` を null に、`lastConfirmed` を今日に、`priority` を同テーマの他 open タスクより少し高く設定

**設計判断**：
- `staleDecide(true)` で `lastConfirmed` を更新するのは、ユーザーの「まだ重要」という回答はタスクが現役である証跡として記録する意味があるため
- `staleDecide(false)` でも `lastConfirmed` を更新するのは、「最後に確認した日」として残しておく方が、後で見直したときに判断材料になるため
- `reviveTask` で `archivedAt: null` を明示的に書き戻すのは、`null` を渡しても YAML 上は `archived_at: null` として記録され、次回読み込み時に `fm.archived_at || null` で正しく null に戻るため

**注意点**：
- `Object.assign(t, updates)` で複数フィールドを一括更新してから `renderTasks` を呼ぶ。UI 更新と Drive 永続化は fire-and-forget（メモリ→UI→バックグラウンド Drive 同期）

### 4.4.3.1 未完了レビューへの進化（2026-07-29 仕様変更）

ユーザーFB：「タスク見直しタイムはカードをフリックする奴だったと思うけど、これはアドホックタスクの方で使うんだよね。時間の設定は不要で、いつでもカードフリックでタスク整理できた方がいい」

これを受け、4.4.3 節で実装した「見直しタイム」を以下の発展系に仕様変更：

**主な変更**：
- `getStaleQueue` を `getUnfinishedQueue` にリネーム
- ロジック変更：`status === "open"` && `!isBlocked`（依存待ち除外）。**`staleReviewDays` 経過の絞り込みを廃止**
- タスクタブにバッジ：未完了タスク数のバッジを表示（赤丸）

**未完了タスクの定義**：
- `status === "open"` かつ `isBlocked` でない（依存待ちタスクは除外）
- 優先度（priority）昇順 → 古い順

**設計判断**：
- 「実行出来ていないタスクを整理」がユーザー要件の中心。N 日以上経過の絞り込みは「いつ整理するか」の時間的条件であって「何を整理するか」の内容的条件ではないため、廃止
- `appSettings.staleReviewDays` 設定項目は互換性のため残す（設定画面に「Task 見直しタイム」として表示中、ただし絞り込みには使われない）

---

### 4.4.3.2 未完了タスク整理 UI のスワイプ化（2026-08-05 確定）

**背景**：
- 旧仕様：Tasks タブ Ad Hoc 上部にスワイプ式カード（1 枚ずつ表示、左右にスワイプで「続ける」「完了」）。goodnight 画面にも同じカードを表示
- ユーザー FB：「未完了タスクの整理は上部のカードで処理するんじゃなくて、一覧を右にスワイプで継続、左にスワイプで保留にするようにしたい」
- **仕様変更**：上部のカード廃止 → タスク一覧の各行にスワイプジェスチャーを付与

**新 UI 仕様**（Gmail 風スワイプ UI）：
- 対象：`status === "open"` かつ `!isBlocked` のタスク行のみ（done / blocked はスワイプ無効）
- **右スワイプ → 完了**：行が右にスライド、左から緑の「✓ 完了」ボタンが現れる。タップで `toggleTaskDone(taskId)` 実行
- **左スワイプ → 保留**：行が左にスライド、右からオレンジの「保留」ボタンが現れる。タップで `holdTaskFromDetail(taskId)` 実行
- **「継続」は何もしない**（スワイプしない = 現状維持）。これは「未完了レビューの『続ける』ボタン廃止」を意味する
- 完了の動線：スワイプ確定 → 完了ボタン表示 → タップ で実行。タスク詳細画面からも完了可能（既存）
- 閾値：80px 以上で確定表示、未満は元に戻る
- ロック中の挙動：行はオフセット位置で固定、アクションラベル以外の場所をタップで解除

**削除されたコード**：
- `renderStaleReview(targetId)` 関数（Tasks / goodnight 両方で利用）
- スワイプ式カード関連の関数：`staleAttachHandlers`, `staleTouchStart/Move/End/Cancel`, `staleMouseStart`, `staleApplyVisual`, `staleResetVisual`, `staleCommitOrReset`, `staleTapDecide`
- CSS：`.stale-review-wrap`, `.stale-review-label`, `.stale-rail`, `.stale-card`, `.stale-hint-*`, `.stale-btn-row`, `.stale-btn`, `@keyframes stale-*`
- HTML：`<div id="stale-review-area">` (Tasks), `<div id="goodnight-stale-review-area">` (goodnight)

**残される機能**：
- `getUnfinishedQueue` 関数：バッジ計算（`updateTaskBadge`）で継続使用
- `holdTaskFromDetail`：タスク詳細画面の保留ボタン + スワイプ保留から呼び出し
- `toggleTaskDone`：既存完了操作 + スワイプ完了から呼び出し
- `appSettings.staleReviewDays`：設定項目として互換性のため残す（ただし未完了タスクの絞り込みには使われない）

---

### 4.4.4 Habit wake/sleep の Drive 永続化（2026-07-28 実装）

`persistTaskChange` パターンと同じ構造で、Habit 用の汎用関数を新設した。

**新設した関数**：
- `updateHabitInDrive(habitId, updates)`：`09_Habit/{id}.md` を取得して frontmatter の指定フィールドだけ書き換え
- `persistHabitChange(habitId, updates)`：Drive 接続チェック + `updateHabitInDrive` を呼ぶ薄いラッパー

**実装した操作**：
- `doGreetAction(kind)` を async 化し、`h.log[d(0)] = ok` の後に `persistHabitChange(h.id, { log: h.log })` を呼ぶ
- `dismissMorningGate` / `openGoodnight` も async 化（`doGreetAction` の await が必要なため）

**設計判断**：
- Habit の `log` は `{ "2026-07-28": true }` のような日付キーのオブジェクトなので、更新時はオブジェクト全体を上書きする（部分マージしない）。日付のキー衝突は当日 1 個だけなので問題なし
- 起床・就寝の OK/NG 判定は従来通りメモリ上で完結し、結果の `log` のみ Drive に書き戻す。判定時刻は Drive に保存しない（毎回判定するため不要）
- 通常 Habit（毎朝フリック）の Drive 永続化は引き続き未着手。`persistHabitChange` 関数は通常 Habit にも転用可能

### 4.4.5 通常 Habit の check_time 切替を Drive 永続化（2026-07-28 実装）

`persistHabitChange` パターンを `setHabitCheckTime`（check_time を morning/night で切り替える操作）に適用した。

**実装した操作**：
- `setHabitCheckTime(id, time)` を async 化し、`h.checkTime = time` の後に `persistHabitChange(h.id, { check_time: time })` を呼ぶ
- メモリ上はキャメルケース（`checkTime`）、Drive 側はスネークケース（`check_time`）。既存の `loadHabitFromDrive`（line 3097）と整合

**設計判断**：
- 既存パターン（`addDep` が `dependsOn` → `depends_on` に明示的に書き換えている、`staleDecide` / `reviveTask` 4.4.3 節）に合わせて、updates に渡すキーを snake_case にした
- メモリ反映時は `camelizeKeys` ヘルパーでキャメルケースに戻して `Object.assign` する。これにより「メモリ上キャメル、YAML 上スネーク」の既存パターンを維持できる

**注意点**：
- 通常 Habit の **Yes/No フリック UI**（仕様書 2.11）は引き続き未実装。フリック UI 自体は次回セッションで設計確認してから着手予定。フリック UI 完成後は、同じ `persistHabitChange` パターンで `log` を Drive に書き戻すだけ

**未着手の関連操作**：
- 通常 Habit（`h-weight`, `h3` など）の Yes/No フリック UI 実装と Drive 永続化
- `special: "weight_training"` の判定ロジック（「トレーニング記録アプリと連携予定（今はサンプル）」と表示）
- `special: "steps"` の数値入力 UI 実装

### 4.4.6 Habit ファイルの自動作成（2026-07-29 実装）

Phase 1 動作テスト（Task 76 検証）で発覚したバグの修正。

**症状**：
- ユーザーは初回 Vault 構築直後で `09_Habit/` フォルダはあるが Habit ファイル（h3.md など）が 1 つも存在しない状態だった
- Daily サブタブで「完了する」を押す → `updateHabitInDrive` が「Habitファイルが見つかりません」で throw → コンソールに「Habit変更のDrive保存に失敗」出力、リロードで消える

**原因**：
- `updateHabitInDrive` は `if(!file) throw new Error(...)` で更新対象の存在を要求していた
- しかし Habit は「Capture 経由で自動作成」するフローがなく、サンプルデータ（`habitData` 初期値）としてメモリ上だけ存在していた
- 初回「完了する」タップで初めて Drive に書き出されるべきだが、そのパスがなかった

**修正**：
- `updateHabitInDrive` の冒頭で `if(!file) { await createHabitInDrive(habitId); return; }` とし、ファイルがない場合は新規作成フローに分岐
- 新規 `createHabitInDrive(habitId)` 関数を追加：メモリ上の `habitData` から該当 Habit を見つけ、必須フィールド（`type: habit`, `id`, `title`, `special`, `check_time`, `themes`, `log`, `wake_time`, `sleep_planned_time`）を持つ YAML フロントマターを構築して `createOrUpdateFile` で書き出す
- 新規ヘルパー `snakeizeKeys` 追加：`camelizeKeys` の逆変換（camel → snake_case）。camelizeKeys と同じく既存パターン「メモリ上キャメル、YAML 上スネーク」を維持

**設計判断**：
- Task 側（`updateTaskInDrive`）も **自動作成に統一**（2026-07-29 修正）。ユーザー動作テストで t-vision.md のような初期サンプルタスクが Drive に存在しないため「タスクファイルが見つかりません」エラーになる問題が発生。`createTaskInDrive` 関数を新設し、Task 側も初回は自動作成フローに
- 初回作成時に `wake_time` / `sleep_planned_time` 等のフィールドが空 dict で書き出される。これは次回起動時の `loadHabitFromDrive` でそのまま読み込まれる（後方互換性のため `|| {}` 保証済み）

**動作テスト**：
- ユーザー：「完了する」タップ → 初回は Habit ファイルが自動作成され、ログ内容が Drive に永続化される
- リロード後：Drive から読み込み、ログが復元される

### 4.4.7 基本の習慣（プリセット）（2026-07-29 実装、2026-08-04 改訂）

起床・就寝・ウェイトトレーニングは **全てのユーザーに行ってほしい習慣**（基本の習慣）。サンプルデータとして開発時に使っていたが、ユーザーの「このプリセットは外せない」という判断（2026-07-29 FB）に基づき、**正式仕様**として実装。

**設計**：
- メモリ上固定のオブジェクト `wakeHabit` / `sleepHabit` / `weightHabit` として定義（Vault とは独立）
- `appSettings.showWakeHabit` / `showSleepHabit` / `showTrainingHabit` で表示/非表示を切り替え（デフォルト true）
- **2026-08-04 改訂**：Tasks タブ Daily サブタブ廃止に伴い、表示場所は **HOME.todaybrief**（起床・就寝時刻欄 + ウェイト実績セクション）に変更。Tasks タブは Ad Hoc のみ
- ウェイト実績は簡略版のみ：今日の完了チェック + 14日カレンダー + 連続日数。旧 Tasks タブ Daily の `renderDailyHabitList` は関数残置（呼び出し箇所なし）
- 朝/夜切替チップ（`habitCheckTimeChip` / `setHabitCheckTime`）は廃止。`weightHabit.checkTime` プロパティも削除
- 設定画面に toggle UI を追加（起床時刻・就寝時刻設定の下に「基本の習慣（プリセット）」セクション）
- `renderDailyHabitList` で toggle に応じてプリセットを `habitData` の前に結合して表示

**判断根拠**：
- ユーザー「ちょっと待って。起床、就寝、トレーニングは基本の習慣だから、プリセットでいいんだよ。すべてのユーザーにこの習慣は行ってほしいから」
- 設定画面からの個別 toggle が必要な理由：今後ユーザーが「今日のところはウェイトはオフにしたい」というケースが想定されるため、ハードコードではなく toggle 式にした

**注意点**：
- プリセット Habit は **メモリ上のみ**。リロードで内容はリセットされる（Phase 3, 4 で Vault 永続化対応予定）
- `appSettings.showWakeHabit` 等の toggle 値もメモリ上のみ。リロードでデフォルト true に戻る（仕様確定後に LocalStorage 永続化対応予定）

### 4.4.8 appSettings の LocalStorage 永続化（2026-07-29 実装）

ユーザーFB：「設定タブの toggle 値がリロードでデフォルトに戻ってしまう」。

- `saveSettingsToLocalStorage()`：`JSON.stringify(appSettings)` を `localStorage.setItem("sb_appSettings", ...)` で保存
- `loadSettingsFromLocalStorage()`：アプリ起動時（`renderTasks("all")` の前）に `Object.assign(appSettings, saved)` で復元
- `saveSettings()` 末尾で `saveSettingsToLocalStorage()` を呼ぶ

**設計判断**：
- Vault ではなく LocalStorage に保存（個人用アプリで同期不要）
- 1 つの JSON でまとめて保存（個別キーより管理が楽）
- エラー時は警告ログのみで動作は止めない（プライバシー設定で LocalStorage 無効のユーザーも考慮）

**注意点**：
- LocalStorage は同一 origin 内で共有。GitHub Pages と localhost で値が共有されない点に注意
- OAuth トークン（googleAccessToken）は LocalStorage に保存しない（セキュリティリスク）

### 4.4.9 未完了レビュー：「続ける」後の同一タスク再表示バグ修正（2026-07-29 実装）

ユーザーFB：「完了にした時は次のカードが表示されるけど、続けるにした時はまた同じカードが表示される」。

**原因**：`getUnfinishedQueue()` が `lastConfirmed` を見ていなかったため、同じ日に「続ける」を選んでも同じタスクが再整理対象になっていた。

**修正**：`getUnfinishedQueue()` に `if(t.lastConfirmed === today) return false;` を追加。今日すでに整理したタスクは本日の整理対象から除外。

**動作**：
- 「続ける」→ `lastConfirmed` が今日に更新 → 明日起きたら再整理対象
- 「完了」→ `status: "done"` → 二度と整理対象外

---

---

## 4.5 （将来構想）筋トレ管理ツール：ダブルプログレッション式レベル算出

現時点では独立したツールとして別途作る予定だが、算出ロジックだけ先に確定させておく。

**前提**：1種目あたり1日2セット、1セットの上限8回。

**状態**：`base`（現在サイクルの軽い方の重量）、`increment`（1単位の重量）、`level`（1〜8）

**8レベルの反復サイクル**
- レベル1〜4：1セット目＝`base+increment`の重量で5→8回まで伸ばす（2セット目は`base`×8回で固定）
- レベル5〜8：1セット目は`base+increment`×8回で固定。2セット目が`base+increment`の重量で5→8回まで伸ばす
- レベル8達成（両セットとも新重量×8回成功）で次サイクルへ：`base = base + increment`、`level`は1に戻る

このサイクルが重量を上げながら無限に繰り返されるため、**個別に定義すべきレベルは1〜8の8つだけ**でよい。

**表示形式**：`(base + increment)kg レベル(1〜8)`

**週次の目標**：週に1回以上レベルアップできていればOKという基準。サイクルをまたぐと数値が7つ下がって見える（レベル8→次サイクルのレベル1）ため、週次の伸びを正しく計算するには、サイクルをまたいでも下がらない**通算レベルアップ回数（monotonic counter）**を別途保持し、そちらの差分で「今週+Nレベル」を算出する。

---

### 4.7.3 ソース入力パイプライン（2026-07-24 実装中・設計判断確定）

ジャーナル入力のパイプライン（4.7.2 ④）を Source 入力に拡張する。既存の `capmode-source` タブを活用し、URL 入力 → 記事本文取得 → Great Mind / Knowledge / Task 抽出 → 確認画面 → Drive 書き込みの流れを実装する。

#### 設計判断（2026-07-24 確定）

| 論点 | 決定 | 理由 |
|---|---|---|
| Great Mind 抽出の粒度 | **複数抽出 OK**（1記事から複数 Great Mind を抽出） | 記事に複数の教えが含まれることが多く、それらすべてを独立ファイルとして保存する方が Knowledge からの参照・引用がしやすい |
| Knowledge alignment | **UI で明示的に選ばせる**（agree / disagree / tension） | 仕様書 4節の「AI自動判定禁止」を厳格に守る。ユーザー自身が判断する |
| YouTube 取得失敗時 | **エラー通知 + 続行可否を聞く** | ユーザーに判断を委ねる。空の Source ファイルだけ保存する選択肢も用意 |
| 既存 Knowledge との統合 | 案B（既存 Knowledge 全件を毎回含める） | 仕様書 4.7.2 で確定済み |
| Capture 画面 Source モード | 既存の `capmode-source` タブをそのまま活用 | UI 設計済みなので再利用する |

#### 実装の制約

- **記事 URL の取得には CORS プロキシが必要**：多くのサイトは CORS を許可していないため、ブラウザから直接 fetch できない。CORS プロキシ（`https://corsproxy.io/?url=` など）を経由する
- **YouTube は API キーが必要**：今日は未対応。記事 URL のみサポート
- **CORS プロキシでも取得できない場合**：タイトル + URL のみ保存し、Knowledge / Task 抽出はスキップ

#### スキーマ（Source 入力で生成されるファイル）

- `02_Sources/{date}_source-{NN}.md`：URL + タイトル + メタ情報 + 本文抜粋
- `06_GreatMind/{date}-source-{NN}-gm-{NN}.md`：Great Mind（複数ファイル生成可）
- `08_Knowledge/{date}-source-{NN}-atom-{NN}.md`：Knowledge（複数ファイル生成可）
- `07_Task/{date}-source-{NN}-task-{NN}.md`：Task（複数ファイル生成可）

各ファイルに `origin_note: {date}-source-{NN}` を設定し、Source ファイルへの参照を保持する。

---

## 4.6 （将来構想）改修案件メモ

**位置づけ**：アプリを実際に使いながら気づいた不具合・改善したいことを、その場でメモしておくための開発者専用機能。ユーザー向けの機能ではなく、開発者設定画面（フッターには出ない、右上の小さな⚙アイコンからのみアクセス）の一番上に置く。

**将来の運用イメージ**：
1. アプリ使用中に気づいたことをその場でメモ・登録する
2. 登録されたメモは、Google Drive Vault内の**管理者用フォルダ**（`00_Inbox`等の通常のNote領域とは別に新設）に保存される
3. 次回Claudeとのチャットで、このフォルダを読みに行き、内容をもとに実際の改修を行う

**現状（Drive連携がまだ未接続の段階）**：
- メモはブラウザのメモリ上にのみ保持される（リロードで消える）
- 「エクスポート」ボタンで、登録済みメモをMarkdownファイルとして実際にダウンロードできる。これをDriveの管理者用フォルダに手動でアップロードするか、次回のチャットにそのまま貼り付けて使う運用とする

**スキーマ（将来Drive連携後）**
```yaml
type: issue_note
id: "..."
created: "（登録日時）"
text: "（メモ本文）"
status: "open" | "resolved"
```

---

## 4.7 次フェーズのロードマップ（2026-07-20時点、更新）

### 4.7.1 Capture抽出プロンプトの詳細仕様（2026-07-20 確定）

前節の①〜③（Note/Knowledge/Task抽出）のMVPに対し、以下を追加で組み込んだ。

| 論点 | 決定内容 |
|---|---|
| Research Task自動生成の閾値 | 新規Knowledgeのconfidenceが**50未満**の場合、自動生成する |
| Branchの新規提案 | Claudeが既存の枝一覧（テーマごと、候補Knowledgeから動的集計）に無ければ**勝手に新しい枝名を作ってよい**。確認画面で強調表示し、ユーザーが却下したら既存の枝から選び直す |
| Great Mind抽出 | **今回のジャーナル用プロンプトの対象外**。Great Mindは主にURL解析（Source入力）から生まれるものなので、Source入力パイプラインを作る時に一緒に実装する。抽出時は「記事の筆者、またはその記事の内容の大本のソースとなった人物」をsageとして判断する方針 |
| KnowledgeのalignmentをAIが自動判定してよいか | **不可。ユーザーが入力の中で明示的に賛成・反対・迷いを述べた場合のみ**記録する。話題として関連していそうというだけでClaudeが推測することは禁止（誤った解釈を防ぐため） |
| 明示的でない、緩い関連性のリンク | 将来的にはやりたいが、**今回は優先度低・対象外** |
| Insight検出（複数Noteを跨ぐパターン） | 1回のCapture処理では原理的に不可能（他のNoteを見ていないため）。**定期的にVault全体を見返す別バッチ処理として切り出す**。検出結果は、朝の門番画面通過後の「おはようメッセージ」でユーザーに伝える案が有力（詳細設計は次回） |

### 4.7.2 次フェーズのロードマップ本体

**②のAPIキー方式は決定済み・動作確認済み**：Cloudflare Workersでキーをサーバー側に隠す方式（`second-brain-proxy-worker.js`）。ユーザーごとの週次予算チェックもKV Namespaceで実装済み。

**①のGoogle Drive実接続**：OAuth・Vault一括アップロード・読み込みパーサー（Library/Task/Knowledge/GreatMind/Habit）まで完成。

**④の書き込み機能：Capture→Claude抽出→Drive書き込みの一連のパイプラインを実装した**（ジャーナル入力のみ。ソース入力はまだ）
- Capture画面のジャーナル入力を、実際のtextarea（iOSキーボードのマイクで音声入力可能）に変更。従来の「タップして話しかける」演出だけのorbは廃止
  - **2026-07-23 実装完了**：orb を撤去し、textarea を最初から表示する形に変更。`openJournalInput()` 関数とそれに伴うリセット処理（保存完了後の orb 再表示）も削除。音声入力は引き続き iOS キーボードのマイクボタンに依存（当面はこの方式で運用）。「会話する感じ」のマイク一発起動は Capacitor 試験導入で再検討する方針（プラン：`C:\Users\takum\.claude\plans\smooth-tickling-perlis.md`）
- **案B方式**：新規入力の前に、既存Knowledge全件（id/title/branch/summary冒頭）と既存Great Mind全件（id/title/sage）を機械的にDriveから取得し、1回のClaude API呼び出しに含める。Claude自身が「新規のKnowledgeか、既存の更新か」「Great Mindへの賛否があるか」を判定する（2段階のAI呼び出しはしていない）
- Claudeの出力は厳密なJSON形式を指定し、`note` / `knowledge`（各々`action`・`branch_is_new`・`alignment`） / `tasks`の3つを1回のレスポンスで受け取る
- 確認画面（journal-confirm）で内容を表示し、「Driveに保存する」を押すと実際に書き込まれる：
  - Knowledge：新規なら`08_Knowledge/`に新規ファイル作成（alignment付与可）、既存更新なら該当ファイルの`evidence`配列に追記（alignmentも追記可）
  - Task：`07_Task/`に新規ファイル作成
  - Note：最後に`01_Journal/`（または`02_Sources/`）に作成し、`knowledge_refs`/`task_refs`で子を参照

**未着手**：
- 🔄 **ソース（URL）入力パイプライン**（2026-07-24 実装中）：ジャーナル入力のパイプラインを踏襲。設計判断は 4.7.3 節に明記
- Exploreのグラフパーサー（Knowledge抽出＋関連性構築、最も複雑。設計は次回持ち越し）
- 定期的なInsight検出バッチ処理（4.7.1参照）
- ✅ **Task完了操作**（2026-07-23 実装完了）：既存UI（チェックボックス）を活用、Drive永続化を `toggleTaskDone` に追加。詳細は 4.4.1 節
- ✅ **Task編集系のDrive永続化**（2026-07-24 実装完了）：`updateTaskInDrive` 汎用関数 + `persistTaskChange` ラッパーを確立。タイトル・本文・Due date・依存タスク編集を Drive に書き戻し。詳細は 4.4.2 節
- ローカル保存オプションの要否検討（未定のまま）
- 案A（Claudeに事前絞り込みさせる2段階呼び出し）との比較検証。今回は案Bのみ実装

---

## 5. 保存形式

**Markdown + YAML frontmatter** で統一する。Obsidian・自作Webアプリのどちらに進んでも流用できるようにするための決定。

### 5.1 将来を見据えた frontmatter 拡張（重要）

これまでのノートは、本文（プレーンな文章）の中にアトム・Taskを埋め込んでいた。これは人間には読みやすいが、将来スクリプトやDBに移行する際、文章から再度アトムを抽出し直す必要が出てしまう。

そこで、**frontmatterの中に構造化データを直接持たせる**ことで、本文は今まで通り人間向けの読みやすい文章のまま、機械的な読み取りはfrontmatterのYAMLだけで完結するようにする。

**追加するフィールド**

| フィールド | 内容 | 理由 |
|---|---|---|
| `id` | このノート固有の安定ID（例：`20260704-journal-03`） | タイトルやファイル名を変更してもリンク切れしないようにするため。related_notesは表示上ファイル名でよいが、内部的にはidで紐付ける |
| `schema_version` | スキーマのバージョン番号（例：`1`） | 将来スキーマを変更した時、古いノートを自動移行できるようにするため |
| `updated` | 最終更新日 | `created`とは別に持つ。特にReference（追記型）ノートで重要 |
| `visibility` | `private` / `shareable` / `public` | 将来の一般公開を見据え、どのノートを公開・共有してよいか最初から持たせておく。Mind系は原則private、Skill/Asset系はshareable候補が多い |
| `knowledge` | このノートから抽出されたアトムを配列として構造化して持つ（下記フォーマット） | 本文の文章とは別に、機械可読なデータとして持たせる |
| `tasks` | Action Task / Research Taskを配列として構造化して持つ | 同上 |

**knowledge配列のフォーマット**
```yaml
knowledge:
  - id: "20260704-atom-01"
    title: "最優先タスクに過集中し、それが未解決の間は他のタスクに集中できない性格特性を持つ"
    themes: ["mind"]
    branch: "性格特性"
    confidence: 85
    source_quality: "Low"
    revises: null
    tags: ["personality", "focus"]
```

**tasks配列のフォーマット**
```yaml
tasks:
  - id: "20260704-task-01"
    type: "action"
    text: "社長・取締役へ退職を報告する"
    due: "2026-07-18"
    status: "open"
  - id: "20260704-task-02"
    type: "research"
    text: "過集中傾向とどう付き合うか、心理学的アプローチを調べる"
    status: "open"
```

これにより、将来Vaultを丸ごとスクリプトでスキャンして、`knowledge`と`tasks`の配列だけを集めれば、そのままDBやグラフJSONに変換できる。本文の文章を解析する必要は一切なくなる。

### 5.2 フォルダ構成
```
00_Inbox/        未整理の書き起こし
01_Journal/      日々のジャーナル（Morning / Walk）
02_Sources/      外部情報（Video / Article / Book / Work）
03_Insights/     複数ノートから見えたパターン・実験ログ
04_MOC/          テーマ別ハブノート（Healthcare / Business / Mind / Relationships）
05_Reference/    スキル・アセットツリー（Skill / Asset）
06_GreatMind/    目標とする理想の思考・人物像（ユーザーが能動的に登録）
Templates/       各typeのひな形
```

---

## 6. 1回の入力処理ごとの標準出力フォーマット

新しい入力を処理するたびに、以下の順で整理する。

1. Source Information（Title / URL / 詳細要約 / Reflection）
2. Knowledge（confidence・source_quality付きのアトム）
3. Summary Knowledge
4. Action Task
5. Research Task
6. （任意）設計・システム改善メモ — このセッションで気づいたルール改善案があれば添える

---

## 7. 明示的に「自動化しない」と決めたポイント

- Insight・実験ログへの昇格判断：固定回数ではなく、Claudeの手動判断＋理由の説明
- スキル・アセットのLv1〜2：ユーザーが決定
- スキル・アセットのLv3の変更：提案ベース、ユーザー確認後に変更

それ以外（似た知識の統合・強化、Lv4以降の再構成、Research Taskの自動生成）は自動でよい。

---

## 8. インフラ・公開ロードマップ（参考）

### 8.1 データアクセス権限と開発者の扱い（2026-07-25 確定）

**目的**：プライバシー保護のため、サービス提供者（開発者）がユーザーのデータを読めない設計にする。

#### 設計方針：**各自の Google Drive に保存**（確定済み）

CLAUDE.md 冒頭「データの保存先はユーザー各自の Google Drive になるように設計してきました」と整合。**リレーショナルDBを持たない**設計を維持する。

#### リフレッシュトークンの保管場所：**ブラウザの localStorage**（設計案 B、2026-07-25 確定）

| 設計案 | 内容 | 採用 |
|---|---|---|
| 設計案 A（Worker Secret） | リフレッシュトークンを Cloudflare Worker の Secret に集約 | ❌ 開発者が他人のトークンを取り出せる |
| 設計案 B（ブラウザ localStorage） | 各ユーザーが自分のブラウザに自分のトークンを保存 | ✅ **採用** |

#### なぜ設計案 B だと開発者が読めないのか

```
┌──────────────────────────────┐
│ ユーザー A のブラウザ         │
│ ┌──────────────────────────┐ │
│ │ リフレッシュトークン A     │ ← A だけが保持
│ └──────────────────────────┘ │
│           │                   │
│           │ アクセストークン │
│           │ （1時間ごと再発行）│
│           ▼                   │
│ ┌──────────────────────────┐ │
│ │ Cloudflare Worker         │ │
│ │ （アクセストークンだけ     │ │
│ │  受け取って API を呼ぶ）  │ │
│ │                          │ │
│ │ ⚠️ リフレッシュトークンは │
│ │    Worker には存在しない！ │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘
```

**Worker がリフレッシュトークンを持たない**ため、開発者が Worker のログや Secret を見ても、ユーザーのリフレッシュトークンは取り出せない。

#### ブラウザ・デバイス変更時の挙動

| シナリオ | リフレッシュトークン | 蓄積データ（Drive） | 必要な作業 |
|---|---|---|---|
| タブを閉じる・再起動 | ✅ 残る | ✅ 残る | なし |
| ブラウザを変える（Edge → Chrome） | ❌ 消える | ✅ 残る | 新しいブラウザで再 OAuth 認証 |
| デバイス変更 | ❌ 消える | ✅ 残る | 新しいデバイスで再 OAuth 認証 |
| 同じ Google アカウントで別ブラウザ | ❌ 再認証必要 | ✅ 共有される | 再 OAuth 認証 |

**重要**：データは Google Drive に保存されているため、同じ Google アカウントを使えば、デバイスやブラウザが変わっても蓄積データを引き継げる。リフレッシュトークンはブラウザ単位でしか引き継げないが、それはセキュリティ上の利点。

#### OAuth クライアントの方針（2026-07-28 確定）

- **個人用（フェーズ0〜1）**：Drive 用と YouTube 用を **別 OAuth Client で運用**（2026-07-28 確定）。
  - 理由：「同じ Consent Screen に `drive.file` と `youtube.force-ssl` を混在させると `cannot be requested together` エラーが発生する」ことが OAuth 問題の原因だったため、**意図的に分離**して解消した（`memory/second-brain-2026-07-28-oauth-resolved.md` 参照）
  - 統合案は 2026-07-25 に検討したが 2026-07-28 に撤回。`memory/second-brain-2026-07-25-morning-session.md` の「Drive 用 Client ID 統一方針」は**未実施**として残している
  - 各 Client は **別 Consent Screen** を持ち、必要スコープのみを登録する：
    - Drive 用 Consent Screen：`drive.file` のみ
    - YouTube 用 Consent Screen：`youtube.force-ssl` のみ
- **マルチユーザー対応（フェーズ2以降）**：スコープ最小化の原則を引き続き適用

#### 開発者の権限まとめ

| アクセス対象 | 開発者は読める？ | 根拠 |
|---|---|---|
| ユーザーの Drive 内の Knowledge / Task ファイル | ✅ **読めない** | リフレッシュトークンを Worker が持たない |
| Anthropic API（Claude 呼び出し） | ✅ 読める（API キーを持つため） | 仕様の範囲内。AI 機能提供のための必須 |
| Cloudflare Worker のソースコード | ✅ 読める | 開発者本人が書いた |
| ユーザーのブラウザ内 localStorage | ✅ 読めない（そもそも開発者の環境に存在しない） | 物理的にアクセス不可 |

#### 詳細な解説

OAuth・localStorage・データアクセス権限の仕組みを図解付きで知りたい場合は `docs/OAUTH_AND_STORAGE.md` を参照。

---

## 9. フェーズ別ロードマップ

| フェーズ | 人数 | 保存先 | 可視化 | 月額目安 |
|---|---|---|---|---|
| 0（現在） | 自分のみ | Obsidian / GitHub private | Claudeにその都度生成してもらう | ¥0 |
| 1 | 〜10人 | 各自のGitHub private repo | GitHub Pages / Cloudflare Pages（静的） | ¥0〜数百円 |
| 2 | 20人〜 | 共有DB（Supabase等） | サーバーホスティング | ここで初めて本格課金 |

**方針**：①データ形式（Markdown）②処理ロジック（本仕様書）③可視化コード（D3ベースのバニラJS）の3つをインフラに依存させないことで、フェーズが進んでも作り直しが発生しないようにする。
