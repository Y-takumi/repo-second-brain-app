# CLAUDE.md — 第二の脳 PKMアプリ

最終更新：2026-08-02（入力タブ改修：フォーカス削除 + 下書き機能 + Library カテゴリフィルタ + 本番環境自動デプロイ確認）

## プロジェクト概要
個人用ナレッジ管理（PKM）Webアプリ「第二の脳」。リレーショナルDBを持たず、Google Drive上のMarkdown（YAML frontmatter付き）ファイル群だけで動作する。音声/URL入力→Claude APIで抽出→Driveに構造化ファイルとして保存、が中核フロー。

**詳細な設計判断・スキーマ・変更の経緯は `00_処理ロジック仕様書.md` を参照。ここには「絶対に外してはいけないルール」だけを書く。**

---

## 絶対ルール（推測せず、必ずこれに従うこと）

### 1. 保存構造：全オブジェクトが独立ファイル
Note・Knowledge・Task・GreatMind・Habitは、**1オブジェクト＝1ファイル**。Note内に配列として埋め込まない（過去に一度この誤りを犯し撤回した経緯がある。詳細は仕様書0.5節）。
- 子オブジェクトは`origin_note`で親Noteのidを持つ
- 親Noteは`knowledge_refs`/`task_refs`/`great_mind_refs`という**IDのみの軽量配列**を持つ（内容は二重管理しない）

### 2. Vaultフォルダ構造
```
00_Inbox/ 01_Journal/ 02_Sources/ 03_Insights/ 04_MOC/ 05_Reference/
06_GreatMind/ 07_Task/ 08_Knowledge/ 09_Habit/ Templates/
```

### 3. APIキー・認証情報の扱い

- **Anthropic API キー**：**絶対にクライアント側コードに書かない**。必ずCloudflare Worker（`second-brain-proxy-worker.js`、KVでユーザーごとの週次予算も管理）を経由する
- **OAuth Client ID**（Google Drive 用、YouTube 用）：**秘匿情報ではない**が、**変数として管理**する。`index.html` の `appSettings.googleClientId` で保持し、開発者設定画面で変更可能にする。コードに直接書き込まない（2026-07-26 方針変更：変数化を推奨）
- **OAuth Client Secret / Refresh Token / API キー類**：**秘匿情報**。Cloudflare Workers の **Secret** として管理し、コードに書かない

### 4. Great MindのKnowledge alignment
KnowledgeのGreat Mindへの賛否（alignment）は、**ユーザーが明示的に述べた場合のみ**記録する。AIが文脈から推測して自動判定することは禁止。

---

## 技術スタック
- フロントエンド：単一の`index.html`（バニラJS、ビルドステップなし）
- 認証：Google Identity Services（`drive.file`スコープ）
- YAML解析：js-yaml
- グラフ描画：D3.js
- API中継：Cloudflare Workers + KV
- デプロイ：GitHub Pages（**main ブランチへの push で自動デプロイ**。URL: `https://Y-takumi.github.io/repo-second-brain-app/`。2026-08-02 確認済み）

## 主要ファイル
- `index.html` — アプリ本体
- `second-brain-proxy-worker.js` — Cloudflare Worker（Claude API中継）
- `00_処理ロジック仕様書.md` — 詳細仕様（設計判断の経緯含む、随時参照）

## 現在の実装状況
Drive OAuth・Vault読み込みパーサー（Library/Task/Knowledge/GreatMind/Habit）・Capture→Claude抽出→Drive書き込み（ジャーナルのみ）まで実装済み。未着手の項目は仕様書4.7節のロードマップを参照。

---

## 進め方の好み
- 大きな設計変更や、既存の決定を覆す提案をする時は、実装前に一度確認する
- ファイルを編集したら、GitHubへのコミットメッセージ案を日本語で添える
- **設計判断が確定したら、会話内だけで終わらせず、必ず`00_処理ロジック仕様書.md`を更新すること**（セッションを切り替えると会話の記憶は失われるため）

### 開発ルール（2026-07-30 追加）

#### 1. 壊れたときの対応
- **新機能を追加する前に、必ず動く状態でコミットする**
- コミットメッセージは「何をしたか」が一目でわかるように書く（例: "検索機能を追加"）
- 追加後に画面が崩れたら、**直そうとせず** `git log --oneline` で履歴を確認する
- 直前のコミットに戻す場合：`git reset --hard <直前のコミットハッシュ>`
- 「〇〇（機能名）を追加する前まで戻して」のように、任意の地点まで遡って戻したい場合：
  1. `git log --oneline` で該当するコミットを探す
  2. 戻す前に `git branch backup-<日付>` で今の状態を退避する
  3. `git reset --hard <該当のコミットハッシュ>` で戻す
- 戻したあとは、原因を分析してから再度実装する
- リモートに合わせる force push は `git push --force-with-lease origin <ブランチ名>` を使う（settings.json に許可設定済み）

#### 2. デザインルール（固定化）
- **余白は狭すぎず広すぎず、統一感を持たせる**（Tailwind なら p-4, gap-4 など決まった単位を使い回す）
- ボタンや入力欄の**角丸・色・サイズは、アプリ全体で同じスタイル**を使う
- 新しいコンポーネントを作るときは、**既存のコンポーネントのスタイルを流用する**（ゼロから作らない）
- レイアウトが崩れていないか、**変更後は必ずスクリーンショットで確認してから次に進む**
- **ボタン内の文字は必ず横に並ぶ**（縦書き禁止）。`white-space: nowrap` + `min-width: 60px` を基本形とする。2026-08-03 夜セッションで HOME タブの「変更」ボタンが横幅不足で「変更」が縦に並んだ事案を受けて追加
- **取り消しの大きい操作（タスク完了等）の成功状態は 1秒間緑背景で表示してから遷移する**：押下前は無彩色（背景透明・枠線のみ）、押下後 1秒間 `.btn-complete.success`（`var(--healthcare)` 緑背景・白文字）で成功を視覚的にフィードバックし、その間に Drive 永続化等の非同期処理を行う。押下後は `disabled` で連続押下防止。2026-08-03 夜セッションで実装


### 動作確認の分担（2026-07-30 追加）
- **UI ビジュアル（見た目・配色・レイアウト）**：Tackman さんが目で確認・判断
- **挙動テスト（想定通り動作するか）**：Claude が Playwright MCP で自動検証
  - 例：朝のフロー実行で wake_time に書き込まれる、バッジのカウントが正しい、Canvas 描画でエラーが出ない、etc
- **詳細手順**：`docs/TESTING.md` の Phase 別テストケースを参照

### 想定通り動作しない場合の対応フロー（2026-07-30 追加）
1. ローカルサーバー再起動（`node .tmp-http-server.js`）
2. ブラウザでハードリロード（Ctrl+Shift+R / Cmd+Shift+R）
3. 1〜2 を繰り返す
4. 改善しなければ調査（`docs/CLAUDE.md` の「未知のシステムを扱う時の調査手順」に従う）
5. 結果を CLAUDE.md か Memory に記録

### Playwright テスト方針（2026-07-30 追加）
- **可能なこと**：起動時 JS エラー、画面遷移、JS 関数の単体実行、Canvas 描画、HTML 構造
- **できないこと**：OAuth 認証画面（Google アカウント必要）、実 Drive 接続・書き込み（環境制約）、実機テスト
- **Phase 5.5 で CDN ローカル化済み**：`vendor/d3.min.js`, `vendor/js-yaml.min.js` を相対パス参照。Playwright で外部 CDN ブロックは発生しない
- **OAuth 不要な単体テストは積極的に Playwright で実行**：朝のフロー（doGreetAction）、夜のフロー（renderTodayBrief）、リズム可視化（renderRhythmChart）など


### 指示の出し方
- **アクションを求めるときは、本当に必要か少し時間がかかっても改めて考えてから指示する**（例：「Dashboard での再設定が必要」と言ったが、`wrangler.toml` に値を完全書いている場合は不要だった、という誤りが過去にあった）
- **実行手順はステップバイステップで具体的に示す**：箇条書きレベルではなく、「どの画面で、どのボタンをクリックして、何を入力して、何を確認するか」まで詳細に。Tackman さんが「A を実行してください」のような単独指示でも、**その前に前提条件と確認手順を示す**
- **勝手な前提を置かない**：「当然こうなっているはず」「普通はこうする」ではなく、実装で確認してから指示する
- **指示を出す前に「なぜそれが必要か」を自問する**：安全性のため？ 動作のため？ 後々困らないため？ 不要なら指示しない
- **誤った認識で情報を伝えた場合、関連するシステムの設定方法を調査してから訂正する**：OAuth のスコープ管理のように、UI のどこで何が設定できるかは実装で確認しないと正確には分からない。推測ではなく、実際にコンソール画面・コード・公式ドキュメントを確認する。**1 度誤ったら「もう間違えない」と意識する**

### 未知のシステムを扱う時の調査手順
- **コードを確認**（Grep、Read）：その値がどこで使われているか
- **公式ドキュメントを確認**（WebSearch、WebFetch）：最新の正確な仕様
- **ユーザーから提供された情報を最優先**：「画面を見た」「セクションがない」「設定項目がない」など、ユーザーの直接確認情報は最も正確。推測より信頼する
- **複数のソースで確認**：コード・ドキュメント・ユーザー観察のうち、最低 2 つで確認する

### Context7 による最新ドキュメント確認
外部ライブラリ・API・クラウドサービスの仕様を扱う時は、記憶で判断せず必ず Context7 で最新ドキュメントを確認してからコーディングやユーザーへの指示を行うこと。
- セットアップ：`npx ctx7 setup --claude`（サインアップ不要の自動セットアップ）または API キー版 `claude mcp add --scope user --header "CONTEXT7_API_KEY: YOUR_API_KEY" --transport http context7 https://mcp.context7.com/mcp`
- 公式：https://github.com/upstash/context7

### 修正履歴の記録
重要な設計判断（OAuth、Worker、ファイル構造など）で誤った認識を伝えた場合、`docs/` 配下の該当ドキュメントに「修正履歴」セクションを追加する。例：`docs/OAUTH_AND_STORAGE.md` に「2026-07-26: OAuth Client のスコープ設定について誤った情報を提供。後日ユーザー観察により訂正」のような記述を追加。

### タスク実施履歴の記録
**すべてのタスク**について以下を `docs/TASK_HISTORY.md` に記録する：

| 項目 | 内容 |
|---|---|
| **タスク ID** | `Task XX` の連番（セッション内で通し番号） |
| **セッション / 日付** | 例：`2026-07-25 朝セッション` |
| **タスク名** | 何をするか |
| **状態** | `pending` / `in_progress` / `completed` / `deleted` |
| **完了時の評価** | `成功` / `問題あり（内容は備考欄に）` / `保留` |
| **備考** | 問題の内容、保留の理由、関連 PR/コミット番号 など |
| **関連コミット** | 対応する Git コミットハッシュ |

**目的**：別のセッションで作業中に問題が発生した場合、`docs/TASK_HISTORY.md` を確認することで、**直近のタスク実施状況から類似の問題や関連する変更を把握**できる。

**新セッション開始時の確認手順**：
1. `docs/TASK_HISTORY.md` を開いて、直近のセッションで実施されたタスクを確認
2. 関連する問題や保留事項があれば、その対応から作業を再開
3. `memory/` の関連メモリ（OAuth デバッグなど）も参照

### セッション切り替え vs compact の判断基準（2026-07-28 追加）

OAuth 問題が 3 日間にわたり長引いた反省から、**セッション切り替えのタイミングを明確化**：

| 状況 | 推奨 | 理由 |
|---|---|---|
| 1 つの機能に集中した短い作業 | compact | 文脈の流れを維持 |
| **複数回の判断ミスがあった** | **セッション切り替え** | 文脈をリセットして再スタート |
| **コンテキスト使用率 70% 超** | **セッション切り替え** | compact で情報が圧縮されすぎる |
| 別の機能・分野に移る | **セッション切り替え** | 認知負荷を下げる |
| **同じ問題で 30 分以上ハマっている** | **セッション切り替え** | 視点を変える必要がある |

**セッション切り替え時の手順**：
1. `docs/TASK_HISTORY.md` の末尾に現在のセッションのタスクを記録
2. `memory/` に必要なら新しいメモを作成
3. 現在のセッションで完了していない重要な決定事項を `docs/` に記録
4. `/exit` または `Ctrl+C` でセッションを終了
5. 新セッションで `claude --continue` または `claude --resume <session-id>` で再開
6. 上記「新セッション開始時の確認手順」に従って確認

### 新セッション開始時の必須ケア手順（2026-07-28 強化）

OAuth 問題が 3 日間にわたり長引いた反省から、**新しいセッションを開始した時は、ユーザーの最初のリクエストにすぐ答える前に、必ず以下の順で確認する**：

1. **プロジェクト全体のルール**：`CLAUDE.md` を最後まで読む
2. **直近のタスク履歴**：`docs/TASK_HISTORY.md` の末尾を見る（直近のセッションで実施されたタスク）
3. **保留中のタスクの確認**：
   - `docs/TASK_HISTORY.md` の「保留中のタスク」セクション
   - 関連 `memory/*.md` の最新ファイル
4. **進行中の大きな問題**：
   - `docs/OAUTH_DEBUG_HANDBOOK.md`（OAuth 関連の問題が現在進行中か）
   - `docs/OAUTH_TROUBLESHOOTING.md`
5. **仕様の確認**：必要に応じて `00_処理ロジック仕様書.md` の関連節

**重要**：確認中はユーザーへの応答は「状況確認中です。1〜2分お待ちください」のように簡潔にする。

確認完了後は、**ユーザーへの最初の応答で「現状認識」を明示する**：
- 「〇〇について作業すると理解しました。直近のタスク履歴では××が完了しています。次は△△に取り組みます。」
- 不確かな部分は「××の状況を確認しましたが、□□は未確認です。明示してください」と確認する。

### ドキュメント更新のタイムスタンプ
すべての重要なドキュメント（`CLAUDE.md`、`00_処理ロジック仕様書.md`、`docs/*.md`、`memory/*.md`）の **冒頭に最終更新日** を記載する。新セッション開始時に確認することで、最新版かどうかが判断できる。
